# SESSION.md

## 2026-07-24 핵심 전제 오류 수정: 붙여넣기는 순차·저속 — 완료까지 대기 후 관리항목 시작
- 사용자가 **붙여넣기 진행 중 실기기 스크린샷 제공**: 001~007행만 채워지고 008행부터 빈칸, 하단 관리항목값 패널 미표시, 합계 0. 즉 ERP는 210행을 한 번에가 아니라 **순차적으로(느리게)** 붙여넣고, **전 행 완료 전에는 관리항목값 칸 자체가 나타나지 않는다.** 내 "한 번에 들어간다" 가정이 틀렸고, 그래서 자동화가 붙여넣기 도중에 관리항목 입력을 시작해 거래처 미변환(GL001 원본 잔존)·미이동·합계 부족(1,015,471,515)이 났다. 직전 48행 실패 스크린샷의 부족 합계가 물증.
- 조치: `_paste_grid_until_reflected()` 직후(Ctrl+Home·관리준비 감지보다 먼저) **하단 차변합계 박스(기본 200,897 150×18) 잉크를 폴링해 합계가 나타나고 안정될 때까지 대기** 후 진행. 붙여넣기 중 합계는 0이라 잉크가 작으므로 `ERP_PASTE_TOTAL_MIN_INK`(기본 40) 문턱으로 "0" 오탐 차단, 연속 `ERP_PASTE_TOTAL_STABLE_COUNT`(기본 3)회 동일(±`ERP_PASTE_TOTAL_INK_TOL` 2)이면 완료. 최초 `ERP_PASTE_TOTAL_MIN_WAIT` 10초 대기, 타임아웃 `ERP_PASTE_TOTAL_TIMEOUT` 600초(초과 시 경고 후 진행), 폴링 `ERP_PASTE_TOTAL_POLL` 2초, 전체 `ERP_WAIT_PASTE_TOTAL_STABLE`로 on/off. 매 폴링 `차변합계 ink=N` 로그를 남겨 좌표 빗나감을 로그로 즉시 확인·보정 가능.
- 회귀 테스트 추가(소스 검증): 대기 블록이 paste→관리항목 사이, Ctrl+Home보다 앞, min_ink 문턱 사용.
- Manager 회귀 136 passed. (커밋/SHA는 배포 후 갱신)
- 다음 시작점: 243 최신 적용 확인 → 재업로드 → **로그에서 `붙여넣기 진행 대기/완료 감지` ink 값 확인**(합계 박스 좌표 검증) → 전 구간 모니터링.

## 2026-07-24 근본 원인 재확정: 붙여넣기 후 그리드가 1행이 아님 → 맨 위 스크롤 추가
- 작업 `793df09f588f`이 1행에서 실패. **업로드된 실패 스크린샷(mgmt_fail_row1)을 직접 확인**: 그리드가 038~061행으로 스크롤돼 있고 038행 선택, 하단 관리항목 패널이 텅 빔. 자동화는 "맨 위=1행"(first_y=231) 가정으로 진행했으나 실제 맨 위는 38행이라, "1행" 클릭이 엉뚱한 곳을 눌러 값이 안 들어감(typed_ink=0).
- 사용자 확인: ERP는 매번 새로(빈 폼) 열고 행추가+붙여넣기만 한다(잔존 전표 없음). 즉 **붙여넣기 후 그리드 스크롤 위치가 비결정적**(가끔 1행, 가끔 38행)이라 이전 실행들은 우연히 1행이었던 것.
- 조치 ① 붙여넣기 후 관리항목 입력 전에 **첫 셀 클릭 + Ctrl+Home으로 그리드를 맨 위(1행)로 강제 스크롤**(`ERP_GRID_SCROLL_TOP_BEFORE_MGMT` 기본 on). 항상 1행부터 시작. ② 직전에 넣었던 과도한 입력 검증(F9 전 값 확인·이름 변환 잉크 판정)은 편집중 반전 렌더링 등으로 정상 입력도 오탐·하드실패시켜 제거. 느린 타이밍(1.2초)+팝업 ESC 방어만 유지.
- Agent 실패 스크린샷 자동 업로드로 처음으로 실제 화면을 확인해 진단(추측 아님).
- Manager 회귀 전체 164 passed. 커밋 `9b59e56`, GitHub `main.zip` Manager SHA-256 `AF2E1EB74AC59E1D2464419C7212925CBB5AFAD68808466519F00406E05C5E83`.
- 다음 시작점: 243 최신 적용 `Manager AF2E1EB74AC5` 확인 → 재업로드 전 구간 검증.

## 2026-07-24 진짜 근본 원인 확정(실기기 스크린샷): 입력이 너무 빨라 거래처 미확정
- 사용자가 실기기 스크린샷 4장 제공. 관리항목값에 거래처가 `이름(번호)`가 아니라 **원본 번호만**(010행 `DJ047`, 014행 `HM025`, 022행 `HA013`) 들어가 있었다. 거래처ds 팝업은 `%GL001` 검색 0건으로 열린 채였다. 사용자 확인: `DJ047`은 실재 거래처번호이고 입력+Enter하면 `거래처명(번호)`로 자동 변환돼야 정상.
- 근본 원인(사용자 진단+코드 확인): 셀 클릭 직후 바로 입력하고 ERP가 값을 받아들이기 전에 F9를 눌러 검색이 빈/불완전 값으로 실행 → 거래처 미확정, 원본 번호만 잔존. **직전 "속도 균일화"에서 입력→F9 대기를 1.0→0.35초로 줄인 것이 악화 요인.** 또한 기존 확정 판정이 "팝업 아니면 확정"이라 원본 번호(작은 잉크)를 확정으로 오판했다.
- 조치(`_input_finance_vendor_code_xy`): ① 입력→F9 대기 0.35→1.20초, 클릭→입력 대기 0.20→0.50초. ② F9 전 값 셀에 입력이 반영됐는지 확인(안 되면 재입력, F9 미전송). ③ Enter 후 잉크 증가(`confirm_ink_margin` 기본 25)로 이름 변환을 확인해 확정 판정 — 원본 번호만 남으면 재확정(기본 2회). 팝업 남으면 ESC 후 재확정. 모두 잉크/키만 사용(스코프 안전).
- Manager 회귀 135 passed(확정/재입력/재확정/팝업 4케이스), 전체 164 passed. 커밋 `e060def`, GitHub `main.zip` Manager SHA-256 `A53B3F98BE610C5B82A3DE46F041FC54114621542EBB619D781097346A86641A`.
- Agent에 실패 스크린샷 자동 업로드도 추가됨(커밋 47a680c) → 다음 실패는 `/api/jobs/{id}/artifacts/mgmt_screenshot`로 확인 가능.
- 다음 시작점: 미저장 전표 폐기 → 243 최신 적용 `Manager A53B3F98BE61` 확인 → 재업로드 전 구간 검증. 확정 대기 강화로 행당 ~5초.

## 2026-07-24 근본 원인: 거래처ds 팝업 미닫힘 → 행별 팝업 닫힘 검증 추가
- 이전 빌드(3AC8490767FA) 실행 `6c0acebb1811`은 미이동 복구가 15번 작동해 60→169행까지 전진했으나 169행에서 최종 실패. 결정적 로그: 복구 성공 행 잉크는 59행 `(25,..)`·144행 `(189,..)`(정상 잔존 값, Down으로 해결)인데, 164/169행은 `ink=(2324,14,166,348)` — 값 셀 97%가 검게 채워진 **거래처ds 팝업이 열린 채 값 셀을 덮은 상태**. Down은 팝업을 못 닫아 169는 3회 재시도 후 실패.
- 근본 원인: 어떤 행의 `F9 → Enter`가 거래처ds 팝업을 못 닫으면(코드 애매/무결과) 다음 행이 그 팝업 위에서 헛돈다. 기존엔 Enter 후 팝업 닫힘을 검증하지 않았다.
- 조치(`_input_finance_vendor_code_xy`): 값 셀 잉크로 상태 구분 — 큰 잉크(≥`ERP_FINANCE_POPUP_INK_MIN` 기본 600)=팝업, 중간=잔존 값, 빈=정상. ① 입력 전 팝업이 남아 있으면 ESC로 닫음. ② Enter 후 팝업이 안 닫혔으면 ESC 후 `F9→Enter` 재확정(`ERP_FINANCE_VENDOR_CONFIRM_RETRIES` 기본 2회). 닫히면 True, 끝까지 안 닫히면 False. 이로써 어떤 행도 팝업을 남기지 않아 cascade 원천 차단. UIA 팝업 탐색 없이 ESC/잉크만 사용(스코프 안전, 입력 함수 로컬만).
- 실패 스크린샷은 각 실패 시 243 `C:\ERP_Auto_Web\excel_voucher_web\data\agent_results\erp_outputs\mgmt_fail_row*.png`에 저장됨(서버 업로드는 아님).
- Manager 회귀 134 passed(팝업 ESC 재확정/미닫힘 실패/입력전 잔존팝업 3개 추가), 전체 163 passed. 커밋 `b592d8e`, GitHub `main.zip` Manager SHA-256 `0FEB0D2F89AD6010E599E1932C55A7D8AE03566620553BA0393B74BFF96C726C`.
- 다음 시작점: 미저장 전표 폐기 → 243 최신 적용 `Manager 0FEB0D2F89AD` 확인 → 재업로드 전 구간 검증.

## 2026-07-24 행 속도 균일화 (사용자 지시)
- 사용자가 1행과 60행 근처 속도가 다르다고 지적하고, 전 미지급금 행을 동일 흐름 `빈칸 클릭 → 입력 → F9 → 1초 대기 → Enter`로 통일 요청.
- 원인: `_advance_grid_row` 마지막 fallback 분기(뷰포트 행 1~23 실제 경로)가 `mgmt_commit_wait`(~0.08~0.3초)를 쓰고, 스크롤 행(24+)은 `finance_row_advance_settle_wait`(2.5초)를 써서 행 전환 속도가 달랐다.
- 조치: ① 뷰포트 행 전환 대기를 `finance_row_advance_settle_wait`로 통일(1~209행 동일 속도). ② 입력 후 F9 전 대기(`finance_vendor_paste_settle_wait`)를 1.00→0.35초로 낮춰 "붙여넣기 후 곧바로 F9"에 맞춤. F9 뒤 1초 대기·Enter는 유지. Enter 후 확정 대기(팝업 닫힘용) 2.00초 유지.
- 실측 검증: 30행 fast 시뮬레이션에서 29개 행 전환이 전부 2.50초, 구 0.08초 0개. 균일 확인. 회귀 테스트로 뷰포트 0.08초 잔존 금지를 고정.
- Manager 회귀 131 passed, 전체 160 passed. 커밋 `4c4d1d7`, GitHub `main.zip` Manager SHA-256 `F06AA58C8D07543A7E739ACD94D640FB1C81F74BCBC327ED0478FAB17E9BA0A3`.
- 입력 방식은 VK_PACKET 주입 유지(클립보드 미사용). 미이동 자동 복구·계좌 물리키·저장 확인창 uia 모두 유지.
- 다음 시작점: 미저장 전표 폐기 → 243 최신 적용 `Manager F06AA58C8D07` 확인 → 재업로드 전 구간 검증.

## 2026-07-24 행 미이동 복구 NameError 수정·복구를 올바른 스코프로 이동
- 직전 배포(502668)가 작업 `e51b1982e406`에서 `name 'row_geometry_state' is not defined`로 60행에서 죽었다. 내 실수 — `_input_finance_vendor_code_xy`(6260행 `_fill_management_items_by_coord` 이전 함수 소속)에 다른 함수의 지역변수 `row_geometry_state`/`summary_x`를 참조하는 재시도 코드를 넣었다. `_load_nested_functions` 테스트가 이름을 주입해 통과시켜 스코프 결함을 못 잡았다.
- 조치: 입력 함수의 재시도를 제거하고 미이동 감지 시 안전 중단(known-good로 복원)했다. 미이동 복구는 요약 앵커/스크롤 상태가 실제 있는 `_fill_management_items_by_coord` 루프로 옮겼다 — 바로 위 "보통예금 행 이동 재시도"와 동일한 방식(같은 스코프 함수·변수만 사용). `거래처번호 직접 키보드 입력` 실패 + 스크롤 모드일 때만 요약 앵커 재클릭+Down 재전송+재선택 후 재입력(기본 3회, `ERP_FINANCE_ROW_ADVANCE_RETRIES`).
- 스코프 회귀 방지 테스트 추가: 입력 함수 본문에 `row_geometry_state`/`_advance_grid_row`가 없고, 복구가 coord-fill 함수 구역 안에 있음을 소스로 검증.
- Manager 회귀 131 passed, 전체 160 passed. 커밋 `b105050`, GitHub `main.zip` Manager SHA-256 `3AC8490767FAEC9056D50106FEFA7250512474975862F633D87CA554E3BD8B9C`.
- 실기기 진전(작업 c9358): 통일 흐름으로 1~59행 정상. 60행 근처 간헐 Down 유실이 남은 과제이며 이번 빌드가 그걸 복구한다.
- 교훈: 이 파일은 `_load_nested_functions` 주입 테스트가 클로저 자유변수 미존재를 못 잡는다. 스코프 밖 이름 참조 금지 + 소스 검증 테스트로 방어.
- 다음 시작점: 미저장 전표 폐기 → 243 최신 적용 `Manager 3AC8490767FA` 확인 → 재업로드 전 구간 검증.

## 2026-07-24 행 미이동 자동 복구(Down 재전송) 추가
- 통일 흐름 첫 실행 `c9358fb2adeb`은 1~59행을 정상 입력했고 60행에서 빈 셀 가드가 미이동(ink=(21,4,10,26))을 감지해 안전 중단·스크린샷 저장했다(과거처럼 헛돌지 않음). 59행 복귀→60행 더블클릭 간격은 2.73초로 행 전환 대기는 정상 적용됐으나, Down 키가 한 번 유실돼 행이 안 넘어간 간헐 현상이었다.
- 조치: 빈 셀 가드가 미이동을 감지하면 하단 스크롤 모드에서 요약 앵커를 다시 클릭하고 Down을 재전송해 스스로 복구한다(기본 3회, `ERP_FINANCE_ROW_ADVANCE_RETRIES`). 복구되면 정상 진행, 재시도 후에도 안 넘어가면 그때 중단. 사람이 하는 것과 같은 단일 복구이며 팝업 탐색·재시도는 추가하지 않는다.
- Manager 회귀 131 passed, 전체 160 passed. 커밋 `df30a11`, GitHub `main.zip` Manager SHA-256 `502668C49E7A492542BF7638B8F9D81084782385D20A0D611D9E4B24BDAC9AB4`.
- 다음 시작점: 미저장 전표 폐기 → 243 최신 적용 `Manager 502668C49E7A` 확인 → 재업로드 전 구간 검증.

## 2026-07-24 1행 특별 흐름 폐지 — 전 미지급금 행 동일 흐름 (사용자 지시)
- 사용자 확정: 1행부터 마지막 미지급금 행까지 전부 동일하게 `관리항목값에 거래처번호 입력 → F9 → Enter`. 1행 전용 거래처ds 팝업 키 시퀀스(Tab4→Down5→Up2→Tab3→Enter2)와 `_seed_vendor_by_number_f9` 호출을 제거했다(정의는 잔존, 호출 없음 — 테스트로 고정).
- 입력은 클립보드 오염 사고 때문에 클립보드 미사용 주입(VK_PACKET)을 유지한다. 빈 셀 검사(클릭 전)·IME 영문 강제·행 전환 2.5초(모든 분기)도 유지.
- Manager 회귀 130 passed, 전체 159 passed. 커밋 `dd91fbe`, GitHub `main.zip` Manager SHA-256 `3C2C744C09D84F925BE92C87F1C7BEBFAB68126E506BF696AD60F5D9386513F5`.
- 다음 시작점: 미저장 전표·팝업 폐기 → 243 최신 적용 `Manager 3C2C744C09D8` 확인 → 재업로드 전 구간 검증.

## 2026-07-24 행 전환 대기가 잘못된 분기에 있던 결함 수정 (근본 원인)
- 작업 `94f7c98284d8` 로그에서 복귀 클릭→다음 적요 더블클릭 간격이 전 구간 0.35초로 확인됐다. 행 전환 2.5초 대기(`ERP_FINANCE_ROW_ADVANCE_SETTLE_WAIT`)가 **최초 경계 진입 분기에만** 있었고, 25행 이후 매 행이 타는 bottom_scroll_mode 분기는 여전히 `mgmt_commit_wait`(약 0.3초)였다. 그동안의 밀림·이동 유실이 "감속 후에도" 재발한 이유다.
- 이번 사고 경로: 76행 Down 유실 → 더블클릭이 75행 재선택 → 셀에 75행 확정값이 남은 채 F9 → 팝업이 `%에코인사이트글로벌(EC008)`로 조회 0건 → 열린 팝업 위에서 121행을 헛돌다 197행 빈 셀 검사에 걸려 종료(저장 없음). 팝업이 화면을 덮으면 빈 셀 검사가 팝업 여백을 보고 통과한다.
- 조치: bottom_scroll_mode 분기의 Down 뒤에도 fast 경로에서는 `finance_row_advance_settle_wait`를 적용. 경계 이후 매 행이 2.5초 대기를 타는지 고정하는 회귀 테스트 추가.
- Manager 회귀 130 passed, 전체 159 passed. 커밋 `c51e4ed`, GitHub `main.zip` Manager SHA-256 `54C82968FFB72F0DCF913E6CBAB42A62B6E835FF0234626D9D9B439EFD0993F7`.
- 다음 시작점: 미저장 전표·거래처ds 팝업 폐기 → 243 최신 적용 `Manager 54C82968FFB7` 확인 → 재업로드 전 구간 검증. 행당 약 2.2초 추가로 전체 약 8분 더 걸린다.

## 2026-07-24 클립보드 오염 사고·클립보드 미사용 입력 확정
- 실기기 화면에서 2행 관리항목값에 다른 거래처의 변환 문자열 `오토에버시스템즈(AT005)`가 붙고 F9 조회가 0건으로 멈춘 사고를 확인했다. 붙여넣기 직전 클립보드가 원격지원 도구/메신저/수동 조작에 의해 오염된 것으로, 243처럼 원격 클립보드가 공유되는 무인 PC에서 붙여넣기는 구조적으로 위험하다.
- 값 입력 방식 최종 확정: ① 거래처 셀 = VK_PACKET 유니코드 입력(클립보드·IME 모두 무관, 다수 실행 검증) ② 거래처ds·계좌 팝업 검색칸 = 물리 키 + 직전 IME 영문 강제(이 칸들은 VK_PACKET을 무시) ③ 값 입력에 클립보드 사용 금지.
- Manager 회귀 130 passed, 전체 159 passed. 커밋 `26861d8`, GitHub `main.zip` Manager SHA-256 `4D91A0E589419BADFE20319C6E38A5D41240D5FCADF902AA0DDD455FAEE3E6A0`.
- 다음 시작점: 진행 중 작업 종료 후 미저장 전표 폐기 → 243 최신 적용 `Manager 4D91A0E58941` 확인 → 재업로드 전 구간 검증.

## 2026-07-24 계좌 검색칸 물리 키 복귀·실패 스크린샷 추가
- 붙여넣기 전환본으로 2회 실행(`1bb62a9942c2`, `276a122878e5`) 모두 1~209행 전 구간 통과(붙여넣기+F9+빈셀검사 흐름 검증 완료), 210행 계좌에서 동일하게 `금융기관지점 자동입력 미확인`으로 중단했다.
- 판정: 계좌 단계가 통과한 유일한 이력(ddb41)은 물리 키 입력본이고 붙여넣기로는 2회 연속 실패 — **계좌 검색칸은 VK_PACKET과 Ctrl+V를 모두 무시하는 컨트롤**이다. 계좌 검색칸만 물리 키 입력으로 복귀했다(직전 IME 영문 강제가 있어 한/영 깨짐 없음). 거래처 셀·거래처ds 검색칸의 붙여넣기는 유지.
- 지점 자동입력 확인 폴링을 2초→6초로 연장(`ERP_MGMT_BANK_BRANCH_CONFIRM_POLLS` 기본 24회×0.25초).
- 무인 진단용으로 관리항목 입력·계좌 지점 실패 시 전체 화면 스크린샷을 `ERP_OUTPUT_DIR`에 자동 저장한다(`mgmt_fail_*.png`).
- Manager 회귀 130 passed, 전체 159 passed. 커밋 `f9a1157`, GitHub `main.zip` Manager SHA-256 `95DF94AE88ACE4663307B9D4E71CB19800C884881A812CC8616F505A42983B7A`.
- 다음 시작점: 미저장 전표 폐기 → 243 최신 적용 `Manager 95DF94AE88AC` 확인 → 재업로드 전 구간 검증.

## 2026-07-23 IME 한글 깨짐 발견·전면 붙여넣기 전환 (사용자 지시)
- 작업 `3450324e88c7`이 다시 2행에서 중단됐고, 사용자가 실기기 화면에서 결정적 원인을 확인했다: 1행 검색칸에 `PT032` 대신 `ㅔㅅ032`가 입력됨 — **243 세션 한/영 IME가 한글 모드**였고 물리 키 입력이 IME를 그대로 타서 자모로 깨진 것.
- 조치 ① IME 강제: 모든 값 입력 직전 `_force_english_ime()`로 IMM32 API를 통해 영숫자 모드 강제(ImmSetConversionStatus + WM_IME_CONTROL). 토글 추측 없음.
- 조치 ② (사용자 지시) 타이핑 전면 폐지: 거래처번호(셀·1행 팝업 검색)와 계좌번호 모두 공용 `_type_vendor_code()`의 **클립보드 붙여넣기**(pyperclip + Ctrl+V)로 통일. 붙여넣기는 IME 상태와 무관하다.
- 조치 ③ 검증 정리: 편집 중 셀은 선택 반전으로 잉크가 안 잡혀 두 번 오탐했으므로 변환 폴링·Enter 재전송을 제거. 밀림 방지 빈 셀 잉크 검사는 **클릭 전(표시 상태)**으로 이동해 유지. 중간 행 확정은 `붙여넣기 → F9 → Enter 1회`.
- Manager 회귀 130 passed, 전체 159 passed. 커밋 `763b048`, GitHub `main.zip` Manager SHA-256 `4BD3FC6B9DA4F7F4B96223D568D2C48B70DD2A0CED652FBCB3495C45A1D38BC7`.
- 다음 시작점: 미저장 전표 폐기 → 243 최신 적용 `Manager 4BD3FC6B9DA4` 확인 → 재업로드 전 구간 검증.

## 2026-07-23 중간 행 F9 확정 전환·스캔 폭 수정 (사용자 지시)
- `5ED8280` 실행이 2행에서 `typed_ink=(0,0,0,0)`으로 중단됐다. 실기기 화면 확인(사용자) 결과 셀에는 `금강기계공업아산지점(KK015)`가 정상 변환돼 있었다 — 입력·Enter는 정상이고 검증 스캔 영역이 새 분할 바 화면의 값 칸 왼쪽 글자를 못 본 것이다.
- 스캔 폭을 250→350px(`ERP_MGMT_VALUE_SCAN_WIDTH`)로 넓혀 구분선이 value_x-360까지 와도 값 칸 전체를 덮게 했다. 변환 대기 기본 4→6초.
- 사용자 지시로 중간 행 확정 흐름을 `거래처번호 입력 → F9 → Enter`로 변경했다(`ERP_FINANCE_VENDOR_F9_WAIT` 기본 1.00초). 빈 셀 검사·이름(코드) 변환 검증·Enter 1회 재전송·감속은 유지. 1행 F9 팝업 흐름은 변경 없음.
- 기존 "중간 행 F9 금지" 규칙은 사용자 지시로 폐지됐다(이 F9는 팝업 탐지가 아니라 조회 실행 키다).
- Manager 회귀 130 passed, 전체 159 passed. 커밋 `1561910`, GitHub `main.zip` Manager SHA-256 `9E9E015298D892A1DACEF202873C7906927DB9A4BCBEA115A54230C6F5601F32`.
- 다음 시작점: 2행까지 입력된 미저장 전표 폐기 → 243 최신 적용 `Manager 9E9E015298D8` 확인 → 재업로드 전 구간 검증.

## 2026-07-23 거래처 Enter 확정 검증·전체 감속 (사용자 지시)
- `Manager 9E28E086BA50` 재실행(분할 바 이동·검색조건 사업자번호 저장 상태)에서 다시 한 행 밀림이 발생해 사용자가 중단했다. 화면 비교로 핵심 신호를 확정했다: Enter가 처리되면 관리항목값이 `이름(거래처번호)` 형식으로 변환되고, 안 되면 원시 코드(`SS230`)로 남는다.
- 사용자 지시로 두 가지를 적용했다. ① 전체 감속: 키 간격 0.15초, 입력 후 1.00초, Enter 확정 2.00초, 행 전환 2.50초(분할 바로 그리드가 커지면 재도색이 느려짐). ② 확정 검증: 입력 전 셀이 비어 있는지 잉크 검사(안 비었으면 행 미이동으로 즉시 중단), Enter 후 이름(코드) 변환을 잉크 폭 증가로 확인(최대 4초, 미변환 시 Enter 1회 재전송, 그래도 안 되면 즉시 중단). `ERP_FINANCE_VENDOR_CONFIRM_TIMEOUT` 기본 4.0초.
- 이 검증으로 밀린 채 수십 행을 계속 쓰는 일은 구조적으로 불가능해졌다. 임의 복구는 Enter 1회 재전송뿐이며 그 외에는 안전 중단한다.
- Manager 회귀 130 passed, 전체 159 passed, 구문 검사 통과. 커밋 `831e643`, GitHub `main.zip` Manager SHA-256 `5ED8280EE9C6CE6F456FA38BB9F36352AD40EEB4DF209E983433EC68F7F4EAF3`.
- 다음 시작점: 밀린 미저장 전표 폐기 → 243 최신 적용 `Manager 5ED8280EE9C6` 확인 → 재업로드 전 구간 검증(예상 소요 30분 안팎).

## 2026-07-23 작업 ddb41e414f03 저장 성공·출력만 실패
- `Manager 9E28E086BA50` 실행에서 1~210행 전체 입력(밀림 없음), 계좌 Enter 2 확정, 금융기관지점 자동입력, **전표 저장까지 성공**했다. 행 전환 대기·계좌 Enter 2 수정이 모두 실기기 검증됐다.
- 실패는 출력 단계뿐이다: 저장 클릭 후 확인창 대기(기본 3초, 실효 약 17초)가 끝난 뒤에야 `자료가 저장되었습니다` 창이 떠서 Ctrl+P(105초)와 전표출력 버튼(63초)이 모달에 막혀 RD Viewer가 열리지 않았다.
- 근본 원인 2가지: ① `_find_erp_message_snapshot`이 win32 backend를 먼저 시도해 WPF Message 창의 문구/버튼을 읽지 못함(`text='Message'`) ② 대형 전표의 저장 확인창 지연 대비 대기 부족.
- 수정: backend 순서를 uia 우선으로 변경(문구 검증·확인 버튼 클릭이 실제로 동작), `ERP_SAVE_CONFIRM_TIMEOUT_SECONDS` 기본 3→60초. 다른 로직 변경 없음.
- Manager 회귀 128 passed, 전체 157 passed, 구문 검사 통과. 커밋 `5ac7304`, GitHub `main.zip` Manager SHA-256 `156E1C66F4382EF6C5E0FAC5590F1324D67A0944C377DA20FD714A3B6B8096F9`.
- 6월 전표는 이미 저장됐으므로 재업로드하면 중복이다. 이번 전표의 출력은 수동(확인 클릭 → 전표출력)으로 마무리하고, 자동 출력 검증은 다음 실행에서 한다.

## 2026-07-23 행 전환 스크롤 대기 추가 (밀림 수정)
- `Manager 5B429776353C` 재실행 중 실기기 화면에서 137행(다온텍 DO007)에 138행 값 `남영자동화익산(NY018)`이 입력되는 한 행 밀림을 확인하고 사용자가 Agent를 수동 중단했다. 전표는 저장하지 않았다.
- 원인: fast 경계 전환에서 Down 후 ERP 스크롤/재도색이 끝나기 전에 다음 적요 더블클릭이 나가면 같은 슬롯에 남아 있는 이전 행을 다시 선택해 이후 거래처가 전부 한 행씩 밀린다. 사용자 판단대로 속도 문제로 처리했다.
- 조치: Down 직후 전용 대기 `ERP_FINANCE_ROW_ADVANCE_SETTLE_WAIT`(기본 1.50초)를 추가했다. 다른 로직·검증 추가 없음.
- Manager 회귀 127 passed, 전체 156 passed, 구문 검사 통과. 커밋 `1271c25`, GitHub `main.zip` Manager SHA-256 `9E28E086BA507A39470E82347F50061CEEBFF57341CD3799AC18D561A40D24E2`.
- 다음 시작점: 밀린 미저장 전표 폐기 확인 → 243 최신 적용에서 `Manager 9E28E086BA50` 확인 → 동일 엑셀 재업로드로 전 구간(1행·중간행 밀림 없음·210행 Enter 2 확정·저장·출력) 검증.

## 2026-07-23 작업 447834c2d663 계좌 확정 Enter 누락
- `Manager D62329FC4478` 실기기 실행에서 1행 `피티테크(PT032)` F9 시퀀스(Down 5→Up 2)와 2~209행 직접 입력이 전부 통과했고 차/대변 합계 1,018,207,215원이 일치했다. 방향키 복원이 실기기에서 검증됐다.
- 210행 계좌 팝업은 검색조건 `계좌번호`, 검색칸 `%140-000-948562`, 정확한 결과행(신한 수원금융센터)까지 도달했지만 첫 Enter가 조회만 실행해 선택 팝업이 열린 채 남았고, 금융기관지점 미검출로 14:59:23 중단했다. 사용자 화면으로 확정 Enter 1회 누락을 확인했다.
- 계좌도 거래처ds와 동일하게 `Tab 4 → Up 2 → Tab 3 → Enter 2`로 확정하도록 수정했다. Enter 간격은 `ERP_MGMT_BANK_F9_ENTER_INTERVAL`(기본 0.40초)로 조정 가능하다.
- Manager 회귀 127 passed, 전체 156 passed, 구문 검사와 `git diff --check` 통과. 커밋 `90c6dca`를 `origin/main`에 push했고 GitHub `main.zip` Manager SHA-256은 `5B429776353C2DC8A41AD884246A7E3886132D0EADF48846CCDD859AA6A930D8`이다.
- 다음 시작점은 243 최신 적용에서 `Manager 5B429776353C` 확인, 미저장 전표 처리 방침 확정(수동 마무리 또는 폐기 후 재업로드), 재실행 시 210행 확정·저장·출력 검증이다.

## 2026-07-23 미검증 Home 폐기·확정 방향키 복원
- 사용자 지시로 실기기 미검증 `Home` 검색조건 선택을 폐기했다. 첫 행 거래처는 `Tab 4 → Down 5 → Up 2 → Tab 3 → Enter 2`, 마지막 계좌는 `Tab 4 → Up 2 → Tab 3 → Enter`의 실기기 확정 키 순서로 복원했다.
- 첫 행·계좌 물리 키 입력, 직접 입력 대기 0.75/1.50초, 중간 행 허위 팝업 가드 제거 등 `5fd2e45`의 나머지 변경은 그대로 유지했다.
- 담당자 프로그램 회귀 127 passed, 전체 `excel_voucher_web/tests` 156 passed, Python 구문 검사와 `git diff --check` 통과. Graphify CLI는 작업 환경에 없어 갱신하지 못했다.
- 커밋 `8b44d08`을 `origin/main`에 push했다. 실제 GitHub `main.zip` Manager SHA-256은 `D62329FC4478F2D82EB1DE3D37C63A13ACDF6AB1C32E8A46F8D4ECBF3506A272`이며 ZIP 원본에서 Down 5/Up 2 복원과 Home 미포함, 물리 키 입력·대기 유지를 확인했다.
- 사용자는 직전 배포 `Manager 7F5344365341`의 243 최신 적용을 완료했고 멈춘 ERP는 저장 없이 닫았다. 다음 시작점은 243 최신 적용에서 `Manager D62329FC4478` 확인 후 `6월 대승 수시결제.xlsx` 재업로드로 1행·중간행·마지막행·저장·출력을 실기기 검증하는 것이다.

## 2026-07-23 첫 행 오입력·전체 입력 속도 재정리
- 실기기에서 1행 적요는 `피티테크(PT032)`였지만 관리항목에는 `현대카드()`가 들어갔다. F9 `거래처ds` 검색칸에 `PT032`가 표시되지 않은 채 전체 목록을 확정한 결과다.
- 운영 구조를 다시 확정했다. DS를 여는 행은 첫 미지급금과 마지막 보통예금뿐이다. 중간 미지급금 행은 관리항목값 셀에 거래처번호를 직접 입력하고 Enter로 확정한다.
- 첫 행 F9 검색값과 마지막 행 계좌번호는 GDI가 VK_PACKET을 무시할 수 있어 `pyautogui.write` 물리 키로 입력한다. 검색값 입력 후 `Tab 4`로 검색조건 콤보에 이동하고 `Home`으로 각각 첫 항목 `거래처번호`/`계좌번호`를 고른 뒤 기존 Tab·Enter 확정을 계속한다.
- 중간 행의 허위 `거래처ds` 판정·중단 코드는 제거했다. 입력 속도 기본값을 거래처 키 간격 0.10초, Enter 전 0.75초, Enter 후 1.50초로 낮췄다. 첫/마지막 F9 검색 대기 2.00초, 키 간격 0.18초, 그룹 대기 0.35초를 사용한다.
- 담당자 프로그램 회귀 127개, 전체 `excel_voucher_web/tests` 156개, Python 구문 검사와 `git diff --check`가 통과했다. Graphify CLI는 작업 환경에 없어 갱신하지 못했다.
- 코드·테스트·상태 문서 커밋 `5fd2e45`를 `origin/main`에 push했다. 실제 GitHub `main.zip` Manager SHA-256은 `7F5344365341BB25A19D8FF6A8A5B2087E9716B67BAA811F89AA7D981A6A330B`이며 ZIP 원본에서 물리 키 거래처 입력, 0.75/1.50초 직접 입력 대기, `Home=거래처번호`/`Home=계좌번호`, 허위 팝업 가드 제거를 확인했다.
- 다음 시작점은 243 최신 적용 화면에서 `Manager 7F5344365341` 확인 후 동일 파일을 재업로드해 1행·중간행·마지막행·저장·출력을 실기기 검증하는 것이다.

## 2026-07-23 거래처 직접 입력 확정 지연 보완
- 후속 실기기에서 1~209행과 210행 이동, F9 계좌 팝업, `계좌번호` 검색조건까지 정상 도달했지만 검색칸에는 `%`만 남고 계좌번호가 입력되지 않았다. 거래처 검색용 `_type_vendor_code`의 VK_PACKET 입력을 계좌 GDI 검색칸이 무시한 재현이다.
- 보통예금 계좌번호만 `pyautogui.write` 물리 키 입력으로 변경했다. 새 팝업의 빈 검색칸에는 `Ctrl+A`를 보내지 않으며 `Tab 4 → Up 2 → Tab 3 → Enter`와 금융기관지점 자동입력 검증은 유지한다. 전용 3 passed, 전체 158 passed.
- 수정 커밋 `6aa2d4d`를 `origin/main`에 배포했다. 실제 GitHub `main.zip` Manager SHA-256은 `821C726BBA84455F3FEF0F9BBF1453B9FD603EE4B1224698D3A3088F9779C533`이다. ZIP 원본에서 물리 키 입력 포함, 계좌 함수의 `Ctrl+A` 및 공용 VK_PACKET 입력 미포함을 확인했다. 다음 시작점은 243에서 `Manager 821C726BBA84` 확인 후 동일 파일 재업로드다.
- 243 최신 적용 후 작업은 2행 직접 입력 직후 `name '_raise_if_vendor_ds_open' is not defined`로 중단했다. 내부 입력 함수와 바깥 행 이동 루프의 중첩 범위를 혼동해 같은 이름을 바깥에서 호출한 것이 원인이었다. 바깥 호출을 제거하고 정의와 같은 범위의 입력 전·Enter 확정 후 검사만 유지했다.
- 작업 `366779ea7b46`은 156행 `DS144` Enter 후 0.091초 만에 다음 행 복귀 클릭을 시작했고, 뒤늦게 열린 `거래처ds` 창을 감지하지 못한 채 210행까지 진행한 것으로 오판했다.
- 2행 이후 미지급금 거래처 직접 입력은 입력 후 대기 0.35초, Enter 확정 대기 1.20초를 기본값으로 사용한다.
- 직접 입력 시작 전, Enter 확정 대기 후, 다음 행 이동 직전에 foreground 제목이 `거래처ds`인지 확인하고 열려 있으면 즉시 중단한다.
- NameError 수정본 `4A1EFCBF25E7`은 210행까지 도달해 반복 거래처 입력 안정화를 확인했지만, 계좌 검색값 미입력 때문에 최종 완료본으로 사용하지 않는다. 다음 시작점은 계좌 물리 키 수정 배포 → 새 243 해시 확인 → 동일 파일 재업로드다.

## 운영 원칙
- 컨텍스트 절약 최우선.
- 긴 분석, 로그, 결정사항, 진행상황은 채팅에 누적하지 말고 MD 파일에 갱신한다.
- 작업 시작 전 관련 MD와 graphify 결과를 먼저 읽는다.
- 필요한 파일만 좁게 열어 진행한다.
- 코드 구조, 의존관계, 영향범위 확인은 graphify를 적극 활용한다.
- 답변은 핵심만 짧게 한다.
- 자세한 인수인계 정보는 MD에 남긴다.
- 작동하던 기존 기능은 임의 삭제 금지.
- 삭제가 필요하면 먼저 질문한다.

## 신규 목표
재정부서 회계업무 자동화 확대 전개.
세금계산서 메일 수신 기반이 아니라, 담당자가 엑셀을 업로드하면 전표 처리를 수행하는 기능을 개발한다.

## 기본 흐름
1. 담당자가 엑셀 업로드.
2. 업로드는 드래그 앤 드롭 또는 파일 선택으로 가능.
3. 업로드 시 회계일 입력/수정 가능.
4. 서버는 작업을 큐에 등록.
5. 담당자는 창을 닫아도 됨.
6. 172.17.30.243 자동 전표처리 PC Agent가 자동으로 ERP 전표 작성/출력 수행.
7. 출력 제출 성공 후 담당자에게 완료 메일 발송.

## 계정/메일 방향
- 그룹웨어 MariaDB `ksystem_yundong`의 `gw_emp`, `ds_t_emp`에서 담당자 계정/메일/부서 정보를 읽는다.
- 외부 DB는 읽기 전용으로만 사용한다.
- 로컬 시스템 초기 비밀번호는 `wowjd12!@`.
- 최초 로그인 및 임시 비밀번호 로그인 시 비밀번호 변경을 강제한다.

## 이번 세션 구현 범위
- 기존 `web_v1` 기능은 수정하지 않는다.
- 새 시스템은 `excel_voucher_web/`에 독립 구성한다.
- 1차 구현은 엑셀 업로드, 전표 payload 생성, 서버 작업 큐, Agent 폴링 API, 담당자용 웹 화면까지 포함한다.
- 실제 ERP 화면 자동입력은 `app/agent_adapter.py`와 `agent/agent_worker.py`에 분리된 후속 구현 지점으로 둔다.

## 2026-07-20 현재 운영 상태
- ERP 하단 관리항목은 화면에 보이면서 UIA에는 `labels=[]`가 되는 GDI 화면이다. 준비 완료는 UIA 글자가 아니라 관리항목 빨간 라벨과 실제 수평 구분선의 안정된 화면 signature 2회로 판정한다.
- 첫 미지급금은 관리항목값 1회 클릭 → F9 → 거래처번호 → Tab 4 → Down 5 → Up 2 → Tab 3 → Enter 2이며, 후속 미지급금은 1회 클릭 → 거래처번호 → Enter다.
- 한 화면의 마지막 완전 표시 행과 전체 행 수는 고정하지 않는다. 실제 구분선 Y로 24/26 등 현재 표시행을 계산하고 payload의 마지막 보통예금까지 같은 경계 상태기계를 사용한다.
- 마지막 보통예금은 최초 F9 성공 좌표를 계좌번호/금융기관지점에 재사용한다. 운영 fast 경로에서는 관리항목명/계정과목 확인을 위한 전체 UIA와 GDI Ctrl+C 검증을 하지 않는다.
- 최종 private 회귀 테스트는 93 passed 상태다. 다음 실기기 실행에서는 로그의 `GDI 하단 관리항목 화면 안정 감지` 직후 1행 F9가 시작되는지 확인한다.

## 2026-07-21 현재 운영 수정
- 1행 진입 전 멈춤의 직접 원인은 화면 준비 감지 이후 실행된 `Desktop.from_point()` 기반 보이는 행 재검사였다. 운영 fast 경로에서는 이 point UIA와 전체 UIA 행 탐색을 호출하지 않고, 이미 검증된 GDI 화면 경계로 만든 1..24/26 좌표를 바로 사용한다.
- 첫 행은 검증된 기존 Y 좌표 `231`을 유지한다. 임시로 검토했던 `240`은 24행 화면을 23행으로 계산할 수 있어 반영하지 않았다.
- 첫 미지급금 행은 관리항목값 1회 클릭 → F9 → 최소 1초 화면 전환 대기 → 거래처번호 → Tab 4 → Down 5 → Up 2 → Tab 3 → Enter 2 순서다. 운영 fast 경로에서는 F9 뒤 UIA 창/자손 열거를 하지 않는다.
- 화면 경계를 지난 뒤에는 현재 화면의 마지막 완전 표시 행 Y를 고정한다. 거래처번호 Enter가 다음 행 이동을 수행한 경우 같은 Y를 재사용하고, Enter가 없는 행만 Down 1회를 보낸다. 총행은 payload 값이므로 230/300행과 마지막 동적 보통예금 행까지 같은 방식으로 진행한다.
- 마지막 보통예금의 기존 계좌번호/금융기관지점 입력 분기는 변경하지 않았다. 전체 회귀 테스트는 95 passed 상태다.

## 2026-07-21 6월 수시결제 재시험 준비
- 첨부 `6월 대승 수시결제.xlsx`는 `수시결제현금` 209건, 전표 210행, 차변/대변 각 `1,018,207,215원`, 변환 경고 0건으로 확인했다.
- 최신 실패 작업 `a89db2f907ac`은 243이 정상 수신했으나 1행 F9 뒤 창 제목/foreground 검증이 ERP 본창 제목 `대승`을 보고 중단했다. 같은 210행 자료의 이전 작업 `2d013d9f6de0`은 해당 검증 추가 전 첫 행부터 마지막 보통예금과 저장까지 통과했다.
- 운영 fast 경로는 이관명세대로 F9 뒤 창 제목/UIA 판정을 하지 않고 최소 1초 대기 후 거래처번호 → Tab 4 → Down 5 → Up 2 → Tab 3 → Enter 2를 수행하도록 복원했다. 비-fast 진단 경로만 기존 창 확인을 유지한다.
- 관련 4개 테스트와 전체 회귀 테스트 `143 passed`를 확인했다. 다음 시작점은 변경 커밋 배포 → `243 최신 적용` 해시 확인 → 위 엑셀 업로드 → 실제 ERP 행 이동/마지막 보통예금 검증이다.
- 수정 커밋 `34317ae`는 `origin/main`에 push 완료했다. 자체 서명 인증서 때문에 자동 브라우저가 차단돼 공식 관리자 API로 전환했으나 현재 관리자 비밀번호가 저장소 기본값과 달라 401로 중단됐다. 아직 `243 최신 적용`과 신규 업로드는 실행하지 않았다. 다음 시작점은 사용자가 관리자 화면에서 로그인 상태를 제공하거나 `243 최신 적용`을 실행한 직후 해시/작업 상태를 확인하는 것이다.
- 2026-07-22 신규 작업 `876c1b0aad14`은 243이 정상 수신했지만 다시 1행 F9에서 실패했다. 서버 보관 로그가 운영 fast 경로에서도 `_wait_vendor_ds_foreground()`를 호출한 것을 보여, 수정본이 아니라 이전 Manager가 실행된 상태로 확정했다. 당시 계산한 `273524745B5A...`는 Windows 작업 파일 해시였고 배포 ZIP 해시는 아니었다.

## 2026-07-22 공개 배포 저장소 동기화
- 243의 최신 적용 결과 `Manager B3830427A6CF`는 수정본 해시와 달랐다. Agent의 기본 업데이트 원본이 private 저장소가 아니라 공개 배포 저장소 `ERP_Auto_Web/main.zip`임을 확인했다.
- 공개 배포 저장소의 Manager가 private 검증본보다 4,053행 이전 상태였으므로 Manager 단일 함수만 이식하지 않고, 검증된 Manager와 결합 실행 파일 `agent_adapter.py`, `agent_worker.py`, `main.py`, 전용 회귀 테스트를 동일 버전으로 동기화했다.
- 첫 F9/이후 행/동적 마지막 은행행 타깃 4건 통과, 원격 최신본 통합 후 공개 배포 저장소 전체 테스트 `142 passed`를 확인했다.
- 배포 커밋 `c7cce27`을 공개 `ERP_Auto_Web/main`에 push했다. Git blob/GitHub ZIP Manager SHA-256은 `DD4E9C3C27889CAAD23851E576F7B931CA30B57E7F78BE38F8AD56D168EEC50F`이며 사용자가 243 완료 화면의 `DD4E9C3C2788`을 확인했다. 다음 시작점은 엑셀 재업로드와 실제 ERP 전 구간 검증이다.

## 2026-07-22 23행 경계 오입력 수정
- 실제 작업 `80a144432336`은 첫 F9 시퀀스와 1~23행까지는 진행했지만, 화면 표시 한계가 `slots=23, last_y=671`인 상태에서 24행부터 관리항목 Enter를 전표 행 이동으로 잘못 간주했다. 이후 Down이 0회여서 24~209행 값과 마지막 `신한 수원금융센터`가 23행 거래처에 반복 덮어써졌다.
- 단일 GDI 구분선 `upper=6, lower=26`은 첫 데이터행이 아니라 관리항목 헤더였다. 경계 계산은 기존 헤더 중심을 유지하고, 실제 클릭 좌표만 한 행 아래로 내려 첫 값행 `y=756`, 두 번째 값행 `y=776`을 사용한다.
- fast 경계 이후에는 관리항목 Enter 여부와 무관하게 현재 적요를 재선택하고 Down 1회를 보내도록 고정했다. 정상 저장도 blind `Ctrl+S → Enter`를 제거하고 검증형 저장 툴바 함수로 통일했다.
- 담당자가 이전 수동 입력에서 분할 바를 움직인 높이는 계정에 저장될 수 있다. 다음 자동 실행은 로그인 후 현재 분할 바를 최초 1회 새로 측정하며, 120px 탐색 밴드 사이에 관리행이 걸려도 겹침 밴드로 찾는다. 10/23/24/26행 화면을 포함해 동적 마지막 표시행을 검증했고, 측정 후에는 실행 종료까지 좌표를 고정한다.
- 핵심 6건과 전체 회귀 테스트 `143 passed`를 확인했다. 수정 커밋 `80298a5`를 `origin/main`에 push했으며 Manager SHA-256은 `CF4C1054572C121E17B7301C2180F5398051A5FB2745CB1AA1D8085026AAF297`이다. 다음 시작점은 243 최신 적용에서 `CF4C1054572C` 확인 후, 현재 잘못 입력된 미저장 전표를 저장하지 않고 닫은 뒤 재업로드하는 것이다.

## 2026-07-22 보통예금 계좌 팝업 수정
- 작업 `9ef5e4e824c9`은 1~209행 미지급금 거래처와 210행 이동까지 정상 완료했다. 210행에서 계좌번호 `140-000-948562`를 입력한 뒤 열린 `계좌` 팝업을 확정하지 않고 금융기관지점 `신한 수원금융센터`를 연속 입력해, 지점명이 계좌 팝업 검색어로 들어갔다.
- 담당자 수동 확인 결과 계좌번호 결과를 선택하면 금융기관지점은 ERP가 자동 입력한다. 계좌 팝업에서 정확 계좌를 검색·선택하고 팝업 종료를 확인하며, 금융기관지점 직접 입력은 생략하도록 수정했다.
- 저장 및 출력 직전에 같은 ERP 프로세스의 미완료 `계좌`/`거래처ds` 팝업을 검사해 남아 있으면 즉시 중단한다. 계좌 관련 19건과 전체 회귀 테스트 `148 passed`를 확인했다. 다음 시작점은 배포 → 243 최신 적용 해시 확인 → 미저장 전표를 닫고 동일 엑셀 재업로드다.

## 2026-07-22 보통예금 계좌 선택 배포
- 수정 커밋 `e03f290`을 공개 배포 저장소 `origin/main`에 push했다.
- 배포 Manager SHA-256은 `065EFC6862EE33E6DF14D521F22F6F347E4737CF13124DBD96EB74C490068E59`이다.
- 다음 시작점은 243 최신 적용 화면에서 `Manager 065EFC6862EE` 확인 후, 현재 수동 입력 전표를 저장하지 않고 닫은 다음 동일 엑셀을 재업로드해 계좌 선택·지점 자동입력·저장·출력을 검증하는 것이다.

## 2026-07-22 작업 f70de9c37945 계좌 직접 확정 분기
- 243은 1~209행 거래처와 210행 계좌번호 `140-000-948562`, 금융기관지점 자동입력까지 정상 완료했다. 다만 계좌 검색조건이 이미 `계좌번호`여서 선택 팝업 없이 직접 확정됐는데, Manager가 팝업 필수로 오판해 저장 전에 중단했다.
- 팝업이 없으면 직접 확정 성공으로 처리한다. 팝업이 열리면 ERP 내부 MDI까지 감지한 뒤 `Tab 4 → Up 2 → Tab 3 → Enter`로 검색조건을 항상 계좌번호에 맞춘다. 금융기관지점 직접 입력은 계속 금지한다.
- 계좌 관련 11건과 전체 회귀 테스트 `155 passed`, Python AST, `git diff --check`를 확인했다. Graphify CLI는 작업공간에서 찾을 수 없어 갱신하지 못했다. 다음 시작점은 배포·243 최신 적용 후 동일 엑셀 재업로드와 저장·출력 실기기 검증이다.
- 수정 커밋 `5bce57f`를 `origin/main`에 push했다. 배포 Manager SHA-256은 `7B8892D8366CD3F371B15CD10ED7528DB82DFE829FD2F34A769D49663A30D8EA`이며, 243 완료 화면의 `Manager 7B8892D8366C` 확인이 다음 시작점이다.

## 2026-07-22 보통예금 F9 강제 선택 변경
- 담당자 최종 지시에 따라 계좌번호 직접확정 분기를 폐기하고 `계좌번호 셀 1회 클릭 → F9 → 계좌번호 입력 → Tab 4 → Up 2 → Tab 3 → Enter` 단일 경로로 변경한다.
- F9 후 계좌 팝업이 열리지 않거나 시퀀스 후 닫히지 않거나 금융기관지점 자동입력 화면을 확인하지 못하면 저장 전에 중단한다. 다음 시작점은 회귀 검증·배포·243 최신 적용이다.
- 계좌 관련 회귀 `10 passed`, 전체 회귀 `154 passed`, Python AST와 `git diff --check`를 확인했다. 금융기관지점 화면 반영은 최대 2초까지 기다린다. Graphify CLI는 작업공간에서 찾을 수 없어 갱신하지 못했다.
- 수정 커밋 `cf9397b`를 `origin/main`에 push했다. 배포 Manager SHA-256은 `64756E54FCD9CEFD7AA5E64BCC27DBD65EE7B1F6F9965A2C0EE7DAB9C488AFDF`다.
- 다음 시작점은 243 최신 적용 화면에서 `Manager 64756E54FCD9` 확인, 현재 미저장 전표 폐기, 동일 엑셀 재업로드다.

## 2026-07-22 작업 826102544bac 내부 계좌 창 감지 지연
- 243은 1~209행을 정상 입력하고 15:21:24에 210행 계좌번호 셀에서 F9를 눌러 `계좌` 내부 창을 실제로 열었다. 그러나 ERP 내부 GDI/UIA descendants 탐색이 약 142초 걸린 뒤 창 없음으로 오판해 15:23:46 실패했다. 작업 관리자에서도 같은 ERP 프로세스 아래 `계좌` 자식 창이 확인됐다.
- F9 입력 경로에서 내부 UIA 창 열거를 제거했다. F9 전후 ERP 전체 화면의 표본 픽셀 변화로 팝업 열림을 확인하고 바로 계좌번호와 `Tab 4 → Up 2 → Tab 3 → Enter`를 입력한다. 이후 화면이 F9 전 상태로 복귀했는지와 금융기관지점 값이 그려졌는지를 확인해야 성공한다.
- 전용 회귀 `11 passed`, 전체 회귀 `155 passed`, Python AST와 `git diff --check`를 확인했다. Graphify CLI는 작업공간에 없어 갱신하지 못했다. 다음 시작점은 배포·243 최신 적용 후 실패 전표를 저장하지 않고 닫고 동일 엑셀 재업로드다.
- 수정 커밋 `3028584`를 `origin/main`에 push했다. 배포 Manager SHA-256은 `89A2EA3A4BD8666B3A7C910DEE94C1A22E5BF03E0835ACC3E3DCEE0DF5E34440`이며, 243 완료 화면에서 `Manager 89A2EA3A4BD8` 확인 후 재업로드한다.

## 2026-07-23 작업 7f1fe41c5bc6 화면 변화 사전검증 폐기
- 신규 작업 `7f1fe41c5bc6`은 243이 08:09:08 수신했고 08:17:39에 95%에서 `210행 계좌번호 F9 후 '계좌' 팝업 화면 변화를 확인하지 못했습니다.`로 종료됐다. 화면 변화율 검증이 실제 입력 전에 다시 중단시켰다.
- 사용자 지시에 따라 팝업 사전/사후 화면 변화율 검증을 폐기했다. `관리항목값 셀 클릭 → F9 → 고정 대기 → 계좌번호 입력 → Tab 4 → Up 2 → Tab 3 → Enter`를 바로 실행한다.
- 성공 기준은 금융기관지점 자동입력 화면뿐이다. 지점 글자가 보이지 않으면 계좌 팝업 상태를 미완료로 유지하고 저장·출력을 차단한다. 전용 회귀 `11 passed`, 전체 `155 passed`, Python AST와 `git diff --check`를 확인했다. Graphify CLI는 없어 갱신하지 못했다.
- 다음 시작점은 배포·243 최신 적용 후 동일 엑셀 재업로드와 계좌번호/금융기관지점/저장·출력 실기기 검증이다.
- 수정 커밋 `cc7e654`를 `origin/main`에 push했다. 배포 Manager SHA-256은 `C7D7CD53B2F58F5F9B76B9FA59AC5609E9F140495EF2B4F9F02EF4D610487998`이며, 243 완료 화면에서 `Manager C7D7CD53B2F5` 확인 후 재업로드한다.

## 2026-07-23 작업 ec565a23bd1e 계좌번호 GDI 키 입력 수정
- 작업 `ec565a23bd1e`은 08:36:21부터 1~209행을 정상 입력하고 08:45:10 F9, 08:45:14 고정 키 순서까지 실행했으나 금융기관지점 미입력으로 08:45:19 중단했다.
- 실기기 화면에서 `계좌` 팝업과 계좌번호 조건은 열렸지만 검색칸에는 `%`만 있고 `140-000-948562`가 없었다. 공용 pywinauto `send_keys(vk_packet=True)`가 GDI 계좌 검색칸에서 무시된 것이 직접 원인이다.
- 보통예금 계좌번호만 `pyautogui.write` 물리 키 입력으로 분리했다. F9·고정 대기·`Tab 4 → Up 2 → Tab 3 → Enter` 및 금융기관지점 최종 검증은 유지한다.
- 전용 회귀 `11 passed`, 전체 `155 passed`, Python AST와 `git diff --check`를 확인했다. Graphify CLI는 없어 갱신하지 못했다. 다음 시작점은 배포·243 최신 적용 후 동일 엑셀 재업로드다.
- 수정 커밋 `5c84075`를 `origin/main`에 push했다. 배포 Manager SHA-256은 `D18D59CB25FB01C9C4F0209D4833E50DE7EC536DB9F8A94D153168F7EDF9305E`이며, 243 완료 화면에서 `Manager D18D59CB25FB` 확인 후 재업로드한다.
- 수정 커밋 `83325f9`를 `origin/main`에 push했다. 배포 Manager SHA-256은 `FABF26AA4DCFA1900E98BA6CE9B83C72C44D533D4AC8B8DFB7299EFBF04EF34C`이며, 243 완료 화면에서 `Manager FABF26AA4DCF` 확인 후 재업로드한다.

## 2026-07-23 마지막 행 입력을 1행 방식으로 통일
- 사용자 재확인에 따라 마지막 보통예금도 첫 미지급금 행과 동일한 공용 `_type_vendor_code()` 입력 방식을 사용한다.
- 별도 `Ctrl+A`와 `pyautogui.write` 물리 키 분기를 제거했다. 차이는 검색값이 계좌번호이고 선택 키가 `Tab 4 → Up 2 → Tab 3 → Enter`라는 점뿐이다.
- 전용 회귀 `11 passed`, 전체 `155 passed`, Python AST와 `git diff --check`를 확인했다. Graphify CLI는 없어 갱신하지 못했다. 다음 시작점은 배포·243 최신 적용 후 동일 엑셀 재업로드다.
