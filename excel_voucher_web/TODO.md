# TODO.md

## 2026-07-23 243 재검증
- [x] 210행 계좌 팝업 검색칸에 `%`만 남은 실기기 증상을 계좌 GDI의 VK_PACKET 무시로 확정한다.
- [x] 계좌번호만 `pyautogui.write` 물리 키로 입력하고 `Ctrl+A`는 보내지 않도록 수정한다.
- [x] 계좌 전용 3개 및 전체 158개 회귀 테스트를 통과한다.
- [x] 243 작업에서 발생한 `_raise_if_vendor_ds_open` NameError의 중첩 범위 원인을 수정한다.
- [x] 팝업 가드 호출이 정의된 내부 입력 함수 범위를 벗어나지 않는 정적 회귀 테스트를 추가한다.
- [x] NameError 수정 커밋 `60e240f`를 배포하고 GitHub ZIP Manager 해시 `4A1EFCBF25E7`을 검증한다.
- [ ] 243 최신 적용 화면에서 `Manager 4A1EFCBF25E7`을 확인한다.
- [x] 반복 거래처 직접 입력의 입력 안정화 대기와 Enter 확정 대기를 늘린다.
- [x] 지연 `거래처ds` 창을 직접 입력 전·확정 후·다음 행 이동 전에 차단한다.
- [x] Manager 전용 및 전체 회귀 테스트 158개를 수행한다.
- [x] 커밋 `ad206a0`을 `origin/main`에 배포하고 GitHub `main.zip` Manager 해시 `B1793FD3E5BF`를 계산한다.
- [x] 243 최신 적용 화면에서 `Manager B1793FD3E5BF` 일치를 확인한다.
- [ ] `6월 대승 수시결제.xlsx`를 다시 업로드해 156행을 통과하고 210행 보통예금 계좌번호/금융기관지점까지 완료되는지 확인한다.

## 엑셀 업로드 전표 처리
- [x] 담당자 엑셀 업로드 UI/API 추가.
- [x] 업로드 시 회계일 입력값 받기.
- [x] 회계일 기본값은 매월 20일.
- [x] 업로드 후 서버 작업 큐 등록.
- [x] 172.17.30.243 자동 전표처리 PC Agent가 ERP 전표 작성 작업을 가져갈 수 있는 API 추가.
- [x] 172.17.30.243 자동 전표처리 PC Agent 자동 실행용 작업 스케줄러 설치 스크립트 추가.
- [x] `5월 대승 수시결제.xlsx` 형식 파서 추가.
- [x] 전표 대상 행은 `수시결제현금` 시트만 사용.
- [x] `수시결제현금` H열 금액 우선 사용.
- [x] 샘플처럼 H열이 순번/합계 불일치일 때만 `인터넷뱅킹` D열을 보조 금액으로 사용.
- [x] ERP 붙여넣기용 `계정과목\t\t차변\t대변\t적요` rows 생성.
- [x] 18080 데이터 서버 전달 옵션 추가.
- [x] 업로드 UI 드래그 앤 드롭 지원.
- [x] 업로드 UI 파일 다이얼로그 지원.
- [x] 담당자용 화면에서 Agent/데이터 서버 등 IT 용어 제거.
- [x] 업로드 영역 확대 및 처리 현황 프로세스바 전면 배치.
- [x] 전산 관리자 전용 작업 멈춤 위치 진단 표시 추가.
- [x] Agent 기본 프린터 출력 제출 단계 추가.
- [x] HTML 파일 인쇄 연결이 없을 때 Notepad 텍스트 출력 fallback 추가.
- [x] Windows 기본 프린터 변경 없이 지정 프린터명 출력 옵션 추가.
- [x] 운영 출력 프린터를 `재정 프린터 (172.16.10.173)`로 전환.
- [x] 출력 전 PDF 보관 파일 생성 추가.
- [x] 계정은 보존하고 작업/업로드만 지우는 초기화 스크립트 추가.
- [x] Agent `real` 모드에서 기존 정기처리 ERP 자동입력 엔진 호출 연결.
- [x] ERP 전표 입력 후 Microsoft Print to PDF 보관 및 지정 프린터 출력 제출 경로 연결.
- [ ] ERP 저장 후 전표번호 자동 회수.
- [ ] ERP 전표 입력/저장/출력 실기기 검증.
- [x] 1행 F9 뒤 이관명세에 없는 창 제목/foreground hard gate 제거 및 전체 회귀 테스트 `143 passed`.
- [x] `243 최신 적용` 완료 화면에서 GitHub 배포 ZIP Manager 해시 `DD4E9C3C2788` 확인(작업 `876c1b0aad14`은 이전 Manager 실행).
- [ ] `6월 대승 수시결제.xlsx`를 실제 업로드해 209개 미지급금과 마지막 보통예금까지 검증.
- [ ] 프린터 선택/용지/매수 설정 별도 설계.

## 담당자별 설정
- [x] 로그인한 담당자별 설정 로드 구조 초안 추가.
- [x] 예: 대승 담당자, 대승정밀 담당자, 일강 담당자.
- [ ] 상세 계정/부서/은행/ERP 입력 규칙 확정.
- [x] 실제 로그인 사용자와 담당자 설정 매핑 기반 추가.
- [x] 그룹웨어 DB `gw_emp`, `ds_t_emp` 읽기 전용 동기화 기반 추가.
- [x] 전산 관리자 계정 기존 WEB DB 동기화 추가.
- [x] 초기 비밀번호 `wowjd12!@` 및 최초 로그인 변경 강제 추가.
- [x] 비밀번호 찾기 임시 비밀번호 발급/메일 발송 훅 추가.
- [x] 그룹웨어 DB 계정/비밀번호 기본값 반영.
- [x] 기존 회계업무 WEB SMTP 기본값 반영.
- [x] 실제 그룹웨어 DB 컬럼명 검증.
- [x] 운영 기본값 `run_server.ps1` 내장.

## 전표 작성 규칙
- [x] 엑셀에 수백 행이 있어도 한 전표 안에 작성.
- [x] 엑셀 행들은 미지급금(원화) 차변으로 입력.
- [x] 마지막에 보통예금 행 1개 추가.
- [x] 보통예금은 대변에 차변금액 총합을 입력.
- [x] 대승 보통예금 관리항목 계좌번호/금융기관지점 1차 확정.
- [x] 미지급금(원화) 관리항목 거래처 및 출납처리여부 해제 규칙 반영.
- [x] GDI `labels=[]` 환경의 관리항목 화면 감지, 24/26 동적 하단선, 첫 행 F9 및 마지막 보통예금 좌표 연결.
- [x] 운영 fast 경로의 1행 진입 전 point UIA 재검사 제거 및 230/300행 동적 경계 회귀 검증.
- [ ] 대승정밀/일강/제이엠 보통예금 계좌번호와 금융기관지점 확정.
- [x] 대승 수시결제 샘플 원본 컬럼명 1차 확정.

## 완료 메일
- [x] Agent 출력 제출 성공 시 완료 메일 발송 훅 추가.
- [x] dry-run 출력만 성공한 경우 완료 메일 미발송으로 변경.
- [x] SMTP 미설정 시 `data/mail_outbox`에 메일 JSON 보관.
- [ ] 운영 SMTP 실제 발송 테스트.

## 2026-07-22 공개 배포
- [x] 검증된 Manager와 결합 실행 파일/테스트를 `ERP_Auto_Web`에 동기화.
- [x] 첫 F9·이후 행·동적 마지막 은행행 타깃 4건 및 원격 최신본 전체 `142 passed` 확인.
- [x] 관련 파일만 커밋 `c7cce27`로 `origin/main`에 push.
- [x] 243 최신 적용 후 Manager `DD4E9C3C2788` 확인.
- [ ] `6월 대승 수시결제.xlsx` 재업로드 후 209개 미지급금과 마지막 보통예금까지 실제 ERP 검증.
- [x] 작업 `80a144432336`의 23행 경계 고정·은행값 덮어쓰기 원인 확정.
- [x] fast 경계 이후 행당 Down 1회, GDI 첫 값행 +20px, 검증형 저장 적용 및 전체 `143 passed`.
- [x] 계정에 저장된 분할 바 높이를 로그인 후 최초 1회 재측정하고 10/23/24/26행 화면 및 탐색 밴드 경계 겹침을 회귀 검증.
- [x] 수정 커밋 `80298a5`를 `origin/main`에 push.
- [ ] 243 최신 적용 후 Manager `CF4C1054572C` 확인.
- [ ] 잘못 입력된 현재 미저장 전표를 저장하지 않고 닫은 뒤 재업로드.
- [x] 작업 `9ef5e4e824c9`에서 209개 미지급금 입력과 210행 이동 정상 확인.
- [x] 계좌번호 선택 시 금융기관지점 자동입력을 확인하고 지점 직접 입력 제거.
- [x] 계좌 검색 결과 확정·팝업 종료 및 저장/출력 전 미완료 팝업 차단 회귀 검증(`148 passed`).
- [ ] 계좌 팝업 수정 배포 후 243 최신 적용 해시 확인 및 동일 엑셀 재검증.
- [x] 계좌 팝업 수정 커밋 `e03f290`을 `origin/main`에 push하고 Manager 해시 `065EFC6862EE` 산출.
- [ ] 243에서 `Manager 065EFC6862EE` 최신 적용 확인 후 동일 엑셀 재업로드로 계좌 선택·지점 자동입력·저장·출력 검증.
- [x] 작업 `f70de9c37945`에서 210행 계좌번호 직접 확정과 금융기관지점 자동입력 확인.
- [x] 계좌 팝업 없음은 금융기관지점 자동입력 화면을 확인해 직접 확정 성공으로 처리하고, 팝업이 열리면 `Tab 4 → Up 2 → Tab 3 → Enter`로 계좌번호 조건을 고정하도록 보완(`155 passed`).
- [x] 새 계좌 직접 확정/조건 고정 수정 커밋 `5bce57f`를 `origin/main`에 push하고 Manager 해시 `7B8892D8366C` 산출.
- [ ] 243에서 `Manager 7B8892D8366C` 최신 적용 후 동일 엑셀 저장·출력 재검증.
- [x] 계좌번호 직접확정 분기를 폐기하고 보통예금도 F9 강제 선택 단일 경로로 변경.
- [x] F9 계좌 선택 변경 회귀 검증(`10 passed`, 전체 `154 passed`) 및 정적 검사 완료.
- [x] F9 계좌 선택 변경 커밋 `cf9397b`를 `origin/main`에 push하고 Manager 해시 `64756E54FCD9` 산출.
- [ ] 243에서 `Manager 64756E54FCD9` 최신 적용 후 동일 엑셀 재검증.
- [x] 작업 `826102544bac` 로그와 작업 관리자에서 F9 계좌 내부 창은 열렸지만 UIA 탐색이 약 142초 지연 후 실패한 원인 확정.
- [x] F9 입력 경로의 내부 UIA 열거를 화면 변화 검증으로 교체하고 전용 `11 passed`, 전체 `155 passed` 확인.
- [x] 화면 변화 검증 수정 커밋 `3028584`를 `origin/main`에 push하고 Manager 해시 `89A2EA3A4BD8` 산출.
- [ ] 243에서 `Manager 89A2EA3A4BD8` 최신 적용 후 기존 실패 전표 폐기, 동일 엑셀 재검증.
- [x] 작업 `7f1fe41c5bc6`에서 화면 변화 사전검증이 210행 입력을 다시 차단한 사실 확인.
- [x] 계좌 팝업 화면 변화 검증을 폐기하고 F9 후 계좌번호·고정 키 순서를 바로 실행하도록 변경(`11 passed`, 전체 `155 passed`).
- [x] 결정적 F9 계좌 입력 커밋 `cc7e654`를 `origin/main`에 push하고 Manager 해시 `C7D7CD53B2F5` 산출.
- [ ] 243에서 `Manager C7D7CD53B2F5` 최신 적용 후 동일 엑셀 재검증.
- [x] 작업 `ec565a23bd1e` 화면·로그에서 계좌 팝업은 열렸지만 검색칸에 계좌번호가 입력되지 않은 원인 확인.
- [x] 보통예금 계좌번호 입력을 pywinauto VK_PACKET에서 `pyautogui.write` 물리 키 입력으로 교체(`11 passed`, 전체 `155 passed`).
- [x] 계좌번호 물리 키 입력 커밋 `83325f9`를 `origin/main`에 push하고 Manager 해시 `FABF26AA4DCF` 산출.
- [ ] 243에서 `Manager FABF26AA4DCF` 최신 적용 후 동일 엑셀 재검증.
- [x] 마지막 보통예금 검색값 입력을 첫 행과 동일한 공용 `_type_vendor_code()`로 통일하고 `Ctrl+A`/물리 키 분기 제거(`11 passed`, 전체 `155 passed`).
- [x] 1행 동일 입력 방식 커밋 `5c84075`를 `origin/main`에 push하고 Manager 해시 `D18D59CB25FB` 산출.
- [ ] 243에서 `Manager D18D59CB25FB` 최신 적용 후 동일 엑셀 재검증.
