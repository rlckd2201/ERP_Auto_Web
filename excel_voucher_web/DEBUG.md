# DEBUG.md

## 주의사항
- 기존 세금계산서 수신/Zoom/정기처리 기능을 임의로 삭제하지 않는다.
- 기존 기능과 신규 엑셀 업로드 전표처리는 분리해서 설계한다.
- Agent 작업 큐, 담당자별 설정, ERP 자동입력 흐름을 먼저 파악한다.
- graphify 결과를 먼저 읽고 관련 파일만 좁게 연다.
- 긴 로그와 시행착오는 이 파일에 갱신한다.

## 2026-06-25 초기 구현 메모
- graphify 리포트 확인 결과 기존 중심은 `ERPAutoApp`, `JobCreateRequest`, Agent 큐 관련 함수다.
- 신규 시스템은 기존 `web_v1`을 직접 수정하지 않고 `excel_voucher_web`에 독립 구성했다.
- 기존 Agent 큐 패턴은 파일 기반이지만 신규 MVP는 SQLite 큐로 시작한다.
- 실제 ERP 입력은 후속으로 `agent/agent_worker.py`의 `run_erp_voucher_task()`에 연결한다.
- Python 실행 파일은 샌드박스 밖 `C:\Users\user\AppData\Local\Programs\Python\Python311\python.exe`에서 확인했다.
- `py_compile` 검증 통과.
- `pytest tests -q` 결과: 1 passed.
- `graphify update .` 완료. 1차 120초 제한에서는 타임아웃, 2차 300초 제한에서 완료됐다.
- 로컬 서버 `http://127.0.0.1:18100` 기동 확인.
- API smoke test 통과: 테스트 엑셀 업로드 200, 큐 상태 `queued`, Agent claim `claimed`, 완료 처리 `done`.

## 2026-06-25 샘플 수시결제 파일 분석
- 파일: `5월 대승 수시결제.xlsx`.
- 시트: `수시결제어음`, `수시결제현금`, `인터넷뱅킹`, `필수확인`.
- `수시결제현금`: 190개 대상 행, 코드 E열, 업체명 F열, 원본 적요 G열.
- `수시결제현금` H열은 data_only 기준 1~190으로 저장되어 실제 금액으로 부적합.
- `인터넷뱅킹`: 190개 이체 행, D열 합계 `1,019,546,751`.
- 승인서 상단 합계 `1,019,546,751원 190(件)`와 `인터넷뱅킹` 합계/건수가 일치.
- 사용자 확인: 전표 작성 대상은 `수시결제현금` 시트에 있는 것만 사용한다.
- 변환기는 `수시결제현금` H열 금액을 우선 사용한다.
- 샘플처럼 H열이 순번으로 저장되어 승인서 합계와 다를 때만 `인터넷뱅킹` D열을 보조 금액으로 같은 순번 매칭한다.
- 샘플 변환 검증 결과: `source_rows=190`, `line_count=191`, `debit_total=1,019,546,751`, `credit_total=1,019,546,751`.
- 샘플 첫 분개 row: `미지급금(원화)		82500000	0	5월 수시결제 - 테스트슈어랩(TS042)`.
- 샘플 마지막 대변 row: `보통예금		0	1019546751	5월 수시결제 - 신한은행`.
- 수정 후 `py_compile` 통과, `pytest tests -q` 결과: 2 passed.
- `graphify update .` 재실행 완료. 1932 files AST extraction, 4645 nodes, 36768 edges.

## 2026-06-25 Agent 접속 타임아웃
- 자동 전표처리 PC에서 `run_agent.ps1 -Server https://172.17.39.121:8081` 실행 시 `/api/agent/heartbeat` 연결 타임아웃 발생.
- 원인 범위: 서버 8081 미기동, 서버 방화벽 8081 미허용, 서버 바인딩/네트워크 경로 문제.
- Agent는 서버 접속 실패 시 Traceback 종료 대신 stderr에 짧게 출력하고 재시도하도록 수정했다.
- 수정 후 `py_compile` 통과, `pytest tests -q` 결과: 2 passed, `graphify update .` 완료.

## 2026-06-25 PowerShell 5.1 스크립트 인코딩
- 서버에서 `run_server.ps1`의 한글 `throw` 메시지가 Windows PowerShell 5.1 기본 인코딩으로 깨져 parser error 발생.
- `run_server.ps1`, `run_agent.ps1`의 throw 메시지를 ASCII 영어로 변경했다.
- `[scriptblock]::Create((Get-Content .\run_server.ps1 -Raw))` 및 `run_agent.ps1` parse 검증 통과.
- `pytest tests -q` 결과: 2 passed.

## 2026-06-25 Agent self-signed SSL 오류
- 자동 전표처리 PC Agent가 `SSLCertVerificationError: self-signed certificate`로 서버 heartbeat 실패.
- 테스트용으로 `agent_worker.py --insecure-skip-tls-verify`와 `run_agent.ps1 -InsecureSkipTlsVerify` 옵션을 추가했다.
- 운영 안정화 시 자동 전표처리 PC에 `C:\ERP_DB\certs\web_v1.cert.pem` 신뢰 등록 후 TLS 검증을 켜고 실행하는 쪽이 더 좋다.
- 수정 후 `py_compile` 통과, `pytest tests -q` 결과: 2 passed, `run_agent.ps1` parse 검증 통과, `graphify update .` 완료.

## 2026-06-26 출력/메일/계정 확장
- 172.17.30.243 프린터 연결 완료 전제로 Agent 출력 제출 단계를 추가했다.
- 현재 출력물은 ERP 자동입력 전 dry-run 전표 HTML 산출물이다. 실제 ERP 화면 입력/ERP 자체 출력은 후속 연결 필요.
- `run_agent.ps1` 기본값은 `-PrintMode default-printer`이며 테스트 시 `-PrintMode off`로 출력 없이 검증 가능하다.
- 웹 업로드 UI는 파일 선택과 드래그 앤 드롭을 모두 지원한다.
- 로그인/세션/비밀번호 해시는 로컬 SQLite에 저장한다.
- 그룹웨어 MariaDB는 `gw_emp`, `ds_t_emp`를 `SHOW COLUMNS`와 `SELECT`로만 읽는다.
- 외부 DB에는 쓰기 SQL을 넣지 않았다. 운영 시 반드시 읽기 전용 DB 계정을 사용한다.
- 신규 동기화 계정 초기 비밀번호는 `wowjd12!@`, 최초 로그인 및 비밀번호 찾기 후 변경 강제.
- 완료 메일은 Agent 결과에 `print_submitted=true`가 있을 때 발송한다.
- SMTP 미설정 시 메일은 `data/mail_outbox` JSON으로 보관된다.
- 검증: `py_compile` 통과, PowerShell scriptblock parse 통과, `pytest tests -q` 결과 5 passed, `node --check app/static/app.js` 통과, `/api/settings` TestClient smoke 200.

## 2026-06-26 기존 메일/DB 기본값 반영
- 그룹웨어 DB 기본 계정은 `dlpadmin2`를 사용하도록 반영했다.
- 기존 회계업무 WEB의 메일 기본값을 참고해 SMTP 기본값을 `35.216.76.148:25`, 발신 주소 `admpdm@dae-seung.co.kr`, 표시명 `재정전표자동화 시스템`으로 맞췄다.
- 기존 전산 코드처럼 SMTP 서버가 STARTTLS를 지원할 때만 TLS를 시작하도록 수정했다.
- 실제 DB 컬럼 확인: `gw_emp.EmpID`, `gw_emp.GwID`, `gw_emp.Use_yn`, `ds_t_emp.EmpID`, `ds_t_emp.kor_name`, `ds_t_emp.bu_code`, `ds_t_emp.DeptName`, `ds_t_emp.use_yn`.
- 재정 하위 부서 활성 계정 조회 결과 17건 확인.
- `gw_emp`에 직접 메일 주소 컬럼은 없어서 `GwID@dae-seung.co.kr` 형식으로 수신 메일을 만든다.

## 2026-06-26 전산 관리자 계정 동기화
- 기존 회계업무 WEB 기본 전산 계정은 `rlckd9646 / 김기창`.
- 새 시스템 시작 시와 전산 계정 로그인 직전에 `C:\ERP_DB\learned_data.db`의 `users` 테이블에서 `rlckd9646` 계정을 다시 읽어 동기화한다.
- 기존 DB에서 읽은 전산 계정은 비밀번호 변경 상태까지 새 시스템 로컬 해시에 반영한다.
- 기존 DB를 읽을 수 없을 때는 새 시스템 로컬 DB에 있는 계정 비밀번호를 유지한다.
- 기존 DB에도 계정이 없을 때만 fallback으로 `eotmd12!@`를 사용한다.

## 2026-06-26 담당자용 UI 정리
- 헤더에서 Agent ID/IP 노출 제거.
- 업로드 영역을 전면 배치하고 드래그 앤 드롭 영역 높이를 확대했다.
- 업로드 시작 즉시 처리 현황 카드에 진행률을 표시한다.
- 작업 큐/데이터 서버/Agent 같은 내부 용어는 화면에서 빼고 접수, 자료 확인, 전표 처리, 출력 단계로 표시한다.
- 검증: `node --check app/static/app.js` 통과, `pytest tests -q` 결과 7 passed.

## 2026-06-26 운영 기본값 스크립트 반영
- `run_server.ps1` 기본값에 그룹웨어 DB 계정, 메일 SMTP, 운영 데이터 폴더를 반영했다.
- 운영 데이터 기본 폴더는 `C:\ERP_DB\excel_voucher_web_data`.
- 이전 PowerShell 세션에 placeholder 환경변수가 남아 있어도 `run_server.ps1` 기본 인자로 덮어쓴다.

## 2026-06-26 자동 전표처리 PC 대기 상태 진단
- 자동 전표처리 PC에서 사람이 별도 수락하는 단계는 없다.
- Agent는 5초마다 서버 `/api/agent/voucher/next`를 호출해 작업을 자동으로 가져간다.
- 운영에서는 172.17.30.243 자동 전표처리 PC 로그인 세션에서 `install_agent_task.ps1`로 Agent 자동 실행 작업을 등록한다.
- 작업이 `queued`/7%에 머무르면 ERP 입력 단계가 아니라 자동 전표처리 PC Agent가 아직 작업을 가져가기 전 상태다.
- 전산 관리자에게만 현재 위치, 확인할 일, 대상 PC, Agent 마지막 접속, 18080 전달 여부, 이벤트 로그를 표시한다.
- 이전 내부 이벤트 문구는 오해를 줄이기 위해 `자동 전표처리 PC 작업 시작`으로 변경했다.

## 2026-06-26 자동 전표처리 PC 출력 fallback
- 172.17.30.243 TEST 계정에서 HTML 파일에 연결된 인쇄 프로그램이 없어 `[WinError 1155]`가 발생했다.
- Agent는 HTML 출력 파일과 별도로 텍스트 전표 파일을 생성한다.
- HTML `print` verb가 실패하면 `notepad.exe /p`로 텍스트 전표를 기본 프린터에 제출한다.
- 이 fallback은 dry-run 출력용이며 실제 ERP 자체 출력은 후속 ERP 자동입력 연결 뒤 별도로 구현한다.

## 2026-06-26 지정 프린터 출력
- 172.17.30.243 프린터 목록에서 운영 출력 대상은 `재정 프린터 (172.16.10.173)`.
- 테스트 완료 후 운영 출력 대상은 `재정 프린터 (172.16.10.173)`로 전환했다.
- Windows 기본 프린터는 변경하지 않는다.
- `run_agent.ps1`, `install_agent_task.ps1`, `agent_worker.py`에 `PrinterName` 옵션을 추가했다.
- `PrinterName`이 있으면 `notepad.exe /pt <file> <printer>`로 지정 프린터 출력한다.
- HTML 전표 파일에서 PDF 보관 파일을 먼저 생성한다. Edge/Chrome headless가 없으면 PDF 보관 실패만 결과에 남기고 출력은 계속 시도한다.

## 2026-06-26 작업 내역 초기화
- 기존 실패 작업은 서버 SQLite `jobs`, `job_events`에 남아 웹에 계속 표시된다.
- DB 파일 전체 삭제는 로그인 계정/비밀번호까지 지우므로 금지한다.
- `reset_jobs.ps1 -ClearUploads -Force`는 계정 테이블을 보존하고 작업 큐/이벤트/업로드 파일만 정리한다.

## 2026-06-26 ERP 관리항목/메일 기준
- 사용자 제공 ERP 원본 화면 기준 미지급금(원화) 관리항목은 `거래처`.
- 미지급금(원화)의 `거래처` 값은 엑셀 업체코드를 우선 사용한다.
- 출납처리여부는 체크 해제해야 한다.
- 대승 보통예금 관리항목은 `계좌번호=140-000-948562`, `금융기관지점=신한 수원금융센터`, `거래처=`이다.
- 대승정밀, 일강, 제이엠 보통예금 계좌/지점은 미확정이므로 `ManagerProfile`에 필드만 준비했다.
- dry-run 출력 성공은 완료메일 발송 조건에서 제외했다.
- 2026-06-26 추가: Agent `real` 모드가 기존 정기처리 `ERPLoginBot`을 호출하도록 연결했다.
- `real` 모드는 ERP 전표를 Microsoft Print to PDF로 먼저 보관한 뒤 지정 프린터로 PDF를 출력 제출한다.
- 현재 전표번호 자동 회수는 아직 없으므로 `voucher_no`가 비어 있고, 완료 메일은 의도적으로 발송되지 않는다.
- 대승 담당자 ERP 회계단위는 `D1공장`으로 1차 설정했다.
- 2026-06-26 재발 방지: 서버가 구형 payload를 보내 `erp_line_management_items`가 없어도 Agent가 `lines[].vendor_code`와 보통예금 기본값으로 관리항목을 복구한다.
- ERP 로그에 `plan=['vendor']`가 보이면 잘못된 구방식 입력이므로 즉시 중지한다. 정상은 `plan=['explicit']` 또는 `명시 관리항목 입력 완료`이다.

## 2026-07-16 미지급금 거래처 MDI 검색 보강
- 기준점은 `main`/`origin/main`/HEAD 모두 `bd8cc1a`, 작업 전 전체 테스트는 22 passed였다.
- 거래처ds 내부 MDI 탐색 시 결과 그리드를 화면 전체 컨테이너로 오인할 수 있어, 상단의 활성 `ComboBox`와 `Edit`를 함께 포함한 후보만 `internal-uia`로 채택하도록 보강했다.
- ERP 메인 창 제목에 거래처 문구가 반영돼도 같은 native handle은 최상위 팝업으로 취급하지 않고 내부 MDI 탐색을 계속한다.
- 검색 버튼은 MDI 내 예상 위치에 가장 가까운 정확한 `검색`/`검색(Fn)` UIA 버튼을 우선 사용하고, 미검출 시에만 기존 좌표 fallback을 사용한다.
- 재정 첫 미지급금 행은 검색 결과 첫 행 더블클릭 후 최대 `ERP_VENDOR_POPUP_RESULT_CLOSE_WAIT`(기본 2.5초) 동안 MDI 닫힘을 확인한다. 일시적인 UIA 누락을 닫힘으로 오인하지 않도록 2회 연속 미검출을 요구한다. 이 전용 경로에서는 Enter fallback을 금지하고 닫히지 않으면 실패로 중단한다. 기존 일반 거래처 검색의 Enter fallback은 유지한다.
- 행동 회귀 테스트로 큰 결과 그리드와 MDI root 구분, 동일 handle 메인 창 제외, 첫 행 검색 순서, 첫 행 Enter 금지, 2행 이후 직접 입력, 마지막 보통예금 계좌/지점 경로 보존을 검증한다.
- 전체 테스트 결과: 31 passed. 공식 Graphify 0.9.16의 `graphify update .` AST-only 갱신 완료(2349 nodes, 6681 edges, 104 communities).
- 실제 ERP PC에서는 첫 행 로그의 `source=internal-uia` 또는 `internal-signature`, `최초 팝업 검색 확정 완료`, 이후 행의 `관리항목값 셀 직접 입력 후 Enter 완료`, 마지막 보통예금 계좌/지점 입력을 확인해야 한다.

## 2026-07-16 미지급금 첫 행 팝업 열기 실패 재수정
- 운영 실패 작업 `68ac4b72e824`의 Agent 로그에서 1행 관리항목 UIA 라벨이 `labels=[]`로 검출돼 `rel=(1118,797)` fallback 셀을 사용한 사실을 확인했다.
- 같은 셀에 기존 `pyautogui.doubleClick(..., interval=0.05)`을 두 번 시도했지만 `거래처ds`가 열리지 않았고, `1행 거래처번호 최초 검색 또는 결과 선택에 실패했습니다.`로 중단됐다. 화면에는 빈 관리항목값 셀의 포커스만 남았다.
- 최초 빈 값 셀과 검색 결과 첫 행은 짧은 synthetic double-click 대신 configurable hold/interval을 둔 좌클릭 `mouseDown`/`mouseUp` 2회로 확정한다. 기본값은 `ERP_VENDOR_DOUBLE_CLICK_HOLD=0.08`, `ERP_VENDOR_DOUBLE_CLICK_INTERVAL=0.18`이다.
- `거래처DS`가 별도 창이 아니라 ERP 메인 창과 같은 native handle의 MDI 제목으로 노출되고 descendants가 비어 있는 경우도 `internal-title`로 인식한다. 일반 ERP 제목은 계속 제외하고, 더 정확한 `internal-uia` 탐색 결과가 있으면 우선한다.
- 회귀 테스트는 실제 down/up 순서, 같은-handle 제목 fallback, 일반 제목 오인 방지, 첫 더블클릭 후 제목이 `거래처DS`로 전환되는 전체 검색 순서를 검증한다.
- 전체 테스트 결과: 34 passed. Python AST 구문 검사 통과. 공식 Graphify 0.9.16의 `graphify update .` 완료(2382 nodes, 6836 edges, 118 communities).

## 2026-07-16 미지급금 첫 행 F9 키보드 입력 전환
- 재정 미지급금(원화) 첫 행에서 빈 관리항목값 셀 더블클릭 및 UIA 검색 버튼/결과 행 선택을 사용하지 않도록 전환했다.
- 첫 행은 관리항목값 셀 1회 클릭 → `F9` → 거래처번호 입력 → `Tab` 4회 → 아래 방향키 5회 → 위 방향키 2회 → `Tab` 3회 → `Enter` 2회 순서로만 처리한다.
- `F9` 직후 기존 내부 MDI 탐지기로 `거래처ds`가 실제로 열린 것을 확인한 뒤 번호와 후속 키를 전송하며, 화면 미검출 시 잘못된 그리드에 키를 보내지 않고 즉시 중단한다.
- 2행부터 마지막 미지급금 행까지는 기존대로 관리항목값 셀 1회 클릭 → 거래처번호 입력 → `Enter` 1회로 처리한다.
- 마지막 보통예금 행의 계좌번호/금융기관지점 입력과 일반 거래처 팝업의 기존 더블클릭 로직은 변경하지 않았다.
- 회귀 테스트는 첫 행의 정확한 클릭/키 순서, F9 화면 미검출 시 중단, 2행 이후 직접 입력, 마지막 보통예금 경로 보존을 검증한다.
- 전체 테스트 결과: 34 passed. Python AST 구문 검사 통과. 공식 Graphify 0.9.16의 `graphify update .` 완료(2382 nodes, 6833 edges, 118 communities).

## 2026-07-16 미지급금 2행 이후 입력 속도 안정화
- 운영 프로필에서 관리항목 붙여넣기 후 `Enter`까지의 실효 대기가 약 0.05초, `Enter` 후 다음 행 이동 시작까지의 명시적 대기가 약 0.21초라 ERP가 값 확정을 마치기 전에 다음 행으로 진행할 수 있었다.
- 전체 관리항목을 느리게 하지 않고, 재정 미지급금(원화) 2행 이후의 거래처 직접 입력에만 전용 안정화 시간을 적용했다.
- 붙여넣기 확정 대기는 `ERP_FINANCE_VENDOR_PASTE_SETTLE_WAIT`(기본 0.10초), Enter 확정 대기는 `ERP_FINANCE_VENDOR_COMMIT_SETTLE_WAIT`(기본 0.16초)로 조정 가능하다.
- 첫 행의 단일클릭 → F9 키보드 순서와 마지막 보통예금의 계좌번호/금융기관지점 입력은 변경하지 않았다.
- 행동 테스트에서 단일클릭 → 붙여넣기 → 0.10초 대기 → Enter → 0.16초씩 확정 대기 순서와 보통예금 기존 인수를 검증한다.
- 전체 테스트 결과: 35 passed. Python AST 구문 검사 통과. 공식 Graphify 0.9.16의 `graphify update .` 완료(2384 nodes, 6837 edges, 118 communities).

## 2026-07-16 전표 그리드 마지막 완전 표시 행 동적 UIA 탐색
- 하단 관리항목 영역 높이에 따라 상단 전표 그리드가 24행 또는 26행까지만 완전히 표시되는데, 기존 로직은 `SS_Row.bottom - 24px`과 고정 행높이로 표시행 수를 한 번 추정하고 운영에서는 `ERP_SKIP_VISIBLE_ROW_SCAN=1`로 행번호 UIA 보정도 건너뛰어 부분 노출 행이나 하단 관리항목 영역을 클릭할 수 있었다.
- 관리항목 입력 시작 직전에 UIA 전표 그리드 스냅샷을 만들고, `SS_Row`, 겹치는 수평 스크롤바, 하단 `관리항목/관리항목값` 헤더를 조합해 실제 그리드 하단 경계를 계산한다.
- 왼쪽 3자리 행번호 라벨을 우선하고 DataItem `CurrentRow`를 fallback으로 사용하며, 실제 행 중심 간격의 중앙값과 행 높이를 측정한다. `행 중심 + 반 행간격`, 노출 높이, viewport 교차 높이를 모두 만족한 행만 완전 표시 행으로 인정해 25행/27행 같은 부분 노출 행은 제외한다.
- 최초 화면에서 `001`행이 확인되지 않으면 이미 스크롤된 상태로 판단해 클릭 전에 중단한다. 마지막 완전 표시 행에서는 `Down` 후 UIA를 다시 확인해 다음 논리 행이 같은 하단 좌표에 완전히 나타난 경우에만 이후 하단 고정 스크롤 모드로 전환한다.
- 보통예금 재시도도 고정 `max_row_y` 대신 현재 UIA 스냅샷의 `last_full_y`를 사용한다. 첫 행 F9, 2행 이후 거래처 입력, 계좌번호/금융기관지점 값 입력 로직은 변경하지 않았다.
- 회귀 테스트는 24행/26행 화면, 원래 크기 및 provider-clipped 부분행, 무명 DataItem/GridItem, 라벨과 상대 DataItem 행번호 충돌, 이미 스크롤된 초기 화면, 실제 경계 refresh/상태 전환과 stale UIA 중단을 검증한다.
- 전체 테스트 결과: 46 passed. Python AST 구문 검사 통과. 공식 Graphify 0.9.16의 `graphify update .` 완료(2397 nodes, 6880 edges, 119 communities).

## 2026-07-16 ERP 메뉴 진입 속도 안정화
- 운영 화면에서 왼쪽 `메뉴` → `회계관리 >>` → 회계관리 타일 → `전표/전표처리/분개전표입력` 전환이 너무 빠르게 이어져 ERP가 다음 입력을 놓칠 수 있었다.
- 관리항목 입력 등 전역 속도값은 변경하지 않고 메뉴 전용 대기만 추가했다. 큰 메뉴 전환은 `ERP_MENU_STEP_WAIT`(0.45초), 전표 트리 이동은 `ERP_MENU_TREE_WAIT`(0.18초), 최종 화면 진입 안정화는 `ERP_MENU_ENTRY_SETTLE_WAIT`(0.30초)로 운영 Agent에서 명시한다.
- ERP 창 최대화 직후와 메뉴·모듈·회계관리 타일 클릭 뒤에는 0.45초, 전표·전표처리 이동과 좌측 메뉴 닫기에는 0.18초, 분개전표입력 UIA/좌표 클릭 및 재시도 Enter 뒤에는 최소 0.30초를 보장한다. UIA가 화면 준비 상태를 즉시 반환해도 최종 클릭 직후의 최소 대기는 생략되지 않는다.
- 첫 행 단일클릭 → F9 키보드 입력, 2행 이후 거래처 직접 입력, 동적 마지막 완전 표시 행 탐색, 마지막 보통예금 계좌번호/금융기관지점 로직은 변경하지 않았다.
- 회귀 테스트는 트리 단계별 대기 순서, 분개전표입력 클릭 후 최소 안정화 대기, Manager 기본값과 운영 Agent 설정 일치, 재시도 Enter 뒤 안정화 순서를 검증한다.
- 전체 테스트 결과: 49 passed. Python AST 구문 검사와 `git diff --check` 통과. 공식 Graphify 0.9.16의 `graphify update .` 완료(2401 nodes, 6890 edges, 113 communities).

## 2026-07-16 DS 전표 메뉴 좌표 오클릭 수정
- 운영 실패 작업 `00ce8c836b2e` 로그에서 기존 DS 좌표 `(105,137) → (126,166) → (155,195)`가 `전표`를 펼치지 못한 채 마지막 클릭으로 `장부`를 여는 것을 확인했다.
- 현재 1920×1040 운영 화면과 회계관리 타일 실측 rect `(234,164)-(374,239)`를 기준으로 메뉴 타일 중앙을 `(304,201)`, 전표 행 중심/간격을 `150/35`로 수정했다.
- DS 메뉴 진입은 UIA 메뉴 클릭을 사용하지 않고 `(30,70) → (145,70) → (304,201)` 좌표로 처리한다. 전표 트리는 `(105,150)` 클릭 후 `Right`, `(126,185)` 클릭 후 `Right`, `(155,220)` 클릭 후 `Enter` 순서로 처리한다.
- 단계 사이에는 기존 메뉴 전용 대기 0.18/0.45/0.30초를 유지한다. 진입 검증 실패 시 다른 메뉴나 폼 셀 오클릭을 막기 위해 추가 좌표 재시도 없이 중단한다.
- 비 DS 법인의 기존 UIA/좌표 fallback과 첫 행 F9, 관리항목 입력, 동적 표시행, 마지막 보통예금 로직은 변경하지 않았다.
- 전체 테스트 결과: 52 passed. Python AST 구문 검사와 `git diff --check` 통과. 공식 Graphify 0.9.16의 `graphify update .` 완료(2405 nodes, 6898 edges, 119 communities).

## 2026-07-20 DS 메뉴 시작 `_env_flag` NameError 수정
- 운영 실패 메일과 ERP Home 빈 화면에서 `메뉴/폼 자동입력 실패: name '_env_flag' is not defined`를 확인했다.
- DS 좌표 전용 여부를 판정하는 `_run_unlocked()`가 `_setup_slip_form()` 내부 지역 함수 `_env_flag`를 잘못 호출해 로그인 직후 메뉴 진입 전에 중단된 것이 원인이었다.
- 메뉴 시작 판정은 해당 범위에서 사용 가능한 `os.getenv("ERP_DS_MENU_COORDINATE_ONLY", "1")` 직접 파싱으로 교체했다. 기존 DS 고정 좌표와 대기시간은 변경하지 않았다.
- 회귀 테스트는 `_run_unlocked()`의 메뉴 초기화부터 `_setup_slip_form()` 호출 전까지 정의되지 않은 bare `_env_flag()` 호출이 없음을 AST로 검증한다.
- 전체 테스트 결과: 53 passed. Python AST 구문 검사와 `git diff --check` 통과. 공식 Graphify 0.9.16의 `graphify update .` 완료(2408 nodes, 6901 edges, 120 communities).

## 2026-07-20 DS 전표 트리 좌표 원복
- 운영 작업 `c2720a590744`에서 `(105,150) → (126,185) → (155,220)` 순서가 `전표처리` 대신 `전표조회`를 펼치고 첫 자식 `전표조건검색`을 실행한 것을 로그와 화면으로 확인했다.
- RDP 축소 화면을 잘못 환산해 기존 좌표 `root_y=137`, `row_h=29`를 `150/35`로 함께 변경한 것이 직접 원인이었다. 기존 137/29 실패 당시에는 현재의 `Right` 강제 방식이 없었으므로 좌표 자체를 원인으로 판단한 것이 잘못이었다.
- 전표 트리는 원래 좌표 `(105,137) → (126,166) → (155,195)`로 복구하고, 각 상위 항목 뒤 `Right`, 마지막 항목 뒤 `Enter` 및 기존 대기만 유지했다. 정상 동작한 회계관리 타일 `(304,201)`은 변경하지 않았다.
- 회귀 테스트는 137/29와 세 클릭 좌표를 고정하고 잘못된 150/35, 185/220 값이 다시 들어오면 실패하도록 보강했다. 관리항목·동적 행·보통예금 로직은 변경하지 않았다.
- 전체 테스트 결과: 53 passed. Python AST 구문 검사와 `git diff --check` 통과. 공식 Graphify 0.9.16의 `graphify update .` 완료(2409 nodes, 6902 edges, 116 communities).

## 2026-07-20 DS 메뉴 클릭 대기 소폭 증가
- 원복한 전표 좌표 `(105,137) → (126,166) → (155,195)`와 `Right → Right → Enter` 순서는 변경하지 않았다.
- ERP가 메뉴 전환을 완료하기 전에 다음 클릭이나 키 입력을 받는 경우를 줄이기 위해 메뉴 전용 대기만 늘렸다: 큰 전환 `0.45 → 0.60초`, 트리 클릭 후 키 입력 `0.18 → 0.25초`, 최종 진입 안정화 `0.30 → 0.40초`.
- 관리항목 입력, F9 첫 행, 동적 표시행, 마지막 보통예금 로직의 속도와 좌표는 변경하지 않았다.
- 전체 테스트 결과: 53 passed. Python AST 구문 검사와 `git diff --check` 통과. 공식 Graphify 0.9.16의 `graphify update .` 완료(2410 nodes, 6903 edges, 122 communities).

## 2026-07-20 210행 전표 관리항목 직전 UIA 정지 수정
- 운영 영상 `20260720_082627.mp4`와 작업 `c9e25a5e43a7`을 대조한 결과 메뉴 진입, 전표 붙여넣기, 1행 표시까지는 정상이었지만 하단 `거래처`가 표시된 뒤 값 입력과 F9까지 도달하지 못한 채 멈춰 있었다.
- 운영 Agent는 `ERP_SKIP_VISIBLE_ROW_SCAN=1`을 설정했지만 이 값이 로그에만 사용되고, 관리항목 입력 시작 시 `_fully_visible_voucher_row_snapshot()`이 `main_win.descendants()` 전체를 동기식으로 열거했다. 210행 전표에서 이 전체 UIA 탐색이 수분 동안 반환되지 않은 것이 직접 원인이었다.
- Excel 붙여넣기 완료 확인에서 이미 확보한 `관리항목`/`관리항목값` 헤더 또는 `SS_Row` viewport의 실제 UIA 하단 좌표를 보존하고, 이 경계와 첫 행 좌표·행 간격으로 완전히 표시된 슬롯을 계산하도록 변경했다. 하단 분할선 위치에 따라 24행 또는 26행을 자동 계산하며, 스크롤 뒤에는 예상 다음 행을 마지막 슬롯에 대응시킨다. Excel COM 복사가 실패해 텍스트 붙여넣기로 전환되는 기존 fallback도 동일하게 준비 완료 스냅샷을 보존한다.
- `ERP_SKIP_VISIBLE_ROW_SCAN=1` 경로에서는 전체 UIA 행 열거를 다시 호출하지 않는다. 헤더 경계가 없거나 비정상이면 느린 전체 탐색으로 되돌아가지 않고 즉시 중단한다.
- 첫 거래처 관리항목 좌표도 붙여넣기 완료 시 확보한 관리항목 스냅샷을 재사용해 같은 전체 UIA 탐색을 반복하지 않는다. 마지막 보통예금의 계좌번호/금융기관지점 처리 분기는 유지했다.
- 회귀 테스트는 24행/26행 경계, 스크롤 후 25행/27행 하단 매핑, 헤더 글자 미검출 시 캐시된 `SS_Row`/수평 스크롤바 경계 사용, 경계 전체 미검출 즉시 중단, 전체 UIA 미호출, 첫 거래처 좌표 캐시 재사용을 검증한다. 전체 테스트 결과: 61 passed.

## 2026-07-20 24/26행 스크롤 고정점 및 마지막 보통예금 정지 수정
- 운영 화면에서 23행까지는 정상 입력됐지만 24행부터 `2행 누락 → 다음 행에 이전 거래처값 입력`이 반복됐다. ERP는 마지막 표시행을 선택하는 순간 한 줄을 미리 스크롤해 선택행을 끝에서 두 번째 슬롯에 고정하는데, 코드는 물리적인 마지막 표시행을 계속 클릭했다. 그 결과 반복마다 `현재 맨 아래 클릭 → Down → 다음 맨 아래 더블클릭`이 각각 스크롤을 발생시켜 실제 선택행이 3행씩 진행됐다.
- 최초 UIA 경계 확인에서 물리적 마지막 표시행은 lookahead로만 보존하고 `slot_ys[-2]`를 실제 스크롤 고정점으로 분리했다. 24행 화면은 23행 좌표, 26행 화면은 25행 좌표가 고정점이며, 해당 경계에 진입한 뒤 210행까지 같은 좌표만 재사용한다. 운영 fast 경로는 전환 시에도 전체 UIA를 다시 열거하지 않는다.
- 210행 보통예금은 행 선택까지 정상인 뒤 `_bank_management_visible()`과 명시 관리항목 검증에서 210행 전체 `main_win.descendants()`를 두 번 호출해 값 입력 전에 정지했다. 운영 fast 경로에서는 현재 계정과목을 클립보드로 `보통예금`인지 확인하고, 첫 거래처 입력 때 이미 확보한 관리항목값 첫 행 좌표를 계좌번호에, 다음 행 좌표를 금융기관지점에 재사용한다. 은행 라벨 확인용 전체 UIA 탐색은 두 곳 모두 생략한다. 출납처리여부도 최초 준비 스냅샷에서 찾은 체크박스 객체를 재사용해 행마다 `descendants()`를 반복하지 않는다.
- 회귀 테스트는 24행/26행 실제 ERP 뷰포트 상태기계를 1행부터 210행까지 진행해 누락·중복·오입력이 없고 최종 화면이 각각 `188~210/210행 고정`, `186~210/210행 고정`인지 검증한다. 보통예금 계좌번호/금융기관지점 좌표와 출납처리여부 캐시 재사용, 전체 UIA 트리 탐색 0회도 검증한다. 전체 테스트 결과: 68 passed.

## 2026-07-20 전표 총행/화면 경계 런타임 보정 및 교대 누락 수정
- 직전의 `slot_ys[-2]` 고정 방식은 ERP가 마지막 적요 더블클릭 또는 거래처값 Enter에서 실제로 언제 스크롤하는지 확인하지 않고 23/25번째 화면 슬롯을 가정했다. 이 때문에 24/26행 화면 변화와 입력 타이밍에 따라 `2행 누락 → 다음 행에 이전 거래처값 입력`이 반복될 수 있어 이 항목의 방식으로 대체했다.
- 최초 준비 스냅샷의 그리드 경계로 완전 표시 슬롯을 계산한 뒤, 왼쪽 행번호 좌표에만 `UIA from_point`를 호출해 실제 `행번호 → Y`를 확인한다. 전체 `descendants()` 재탐색은 사용하지 않는다. `GridItem.CurrentRow`보다 ERP에 표시된 3자리 이상 행번호 라벨을 우선하며 짧은 viewport 상대 숫자는 제외한다.
- 계산된 마지막 적요를 더블클릭한 직후 current/next 행 Y를 저장하고, 거래처번호 Enter 뒤 현재 행 Y 변화 또는 다음 행의 `SelectionItem.CurrentIsSelected`를 한 번 확인한다. Enter 이동이 확인되면 다음 행의 실제 Y를 `enter` 모드로, 확인되지 않으면 현재 행에서 Down 1회 후 얻은 실제 Y를 `down` 모드로 고정한다. 이후에는 같은 Y와 판정된 이동 방식만 재사용한다.
- 경계 직전 거래처 Enter가 이미 화면을 움직인 경우에도 다음 행번호 지점을 다시 읽어 stale 초기 좌표로 다른 행을 더블클릭하지 않는다. 행번호 지점 확인에 실패하면 추정 좌표로 계속하지 않고 즉시 중단한다.
- 총행은 210으로 고정하지 않고 payload의 `rows_to_fill`을 그대로 사용한다. 24/26 표시행 × 총 230/300행 × Enter/Down 분기에서 1행부터 마지막 행까지 순서가 누락되지 않고, 동적 마지막 보통예금 행에서 기존 계좌번호/금융기관지점 두 값이 입력되는지 검증했다.
- 첫 행 단일클릭 → F9 → 거래처번호 → Tab/방향키/Enter 순서, 2행 이후 관리항목값 단일클릭 → 거래처번호 → Enter, DS 메뉴 좌표/대기, 마지막 보통예금 fast 좌표는 유지했다.
- 전체 테스트 결과: 74 passed. Python AST 구문 검사와 `git diff --check` 통과. 공식 Graphify 0.9.16의 `graphify update .` 완료(2436 nodes, 6992 edges, 121 communities).

## 2026-07-20 GDI 행번호 point UIA 미노출 사전중단 수정
- 운영 작업 `3a5f6c5f448b`은 `ERP_PASTE!A1:E210`을 정상 붙여넣었고 원본 209행 뒤 보통예금 210행까지 ERP에 표시됐다. 실패는 은행값 입력이 아니라 관리항목 루프 직전 추가된 행번호 point UIA 검증이었다. 준비 스냅샷은 `slots=24, logical=1..24`를 정상 계산했지만 GDI 화면의 `Desktop.from_point()`가 행번호를 노출하지 않아 `targets=[1,24], rows=[]`가 되었고 기존 hard gate가 즉시 중단했다.
- 초기 point UIA 결과는 필수 조건이 아닌 보정 기능으로 변경했다. 캐시된 `SS_Row`/관리항목 경계로 검증된 1..24/26 좌표 맵을 보존하고, point UIA 결과가 있으면 병합하며 없으면 geometry 맵으로 계속 진행한다. geometry에도 목표 좌표가 없을 때만 기존처럼 입력 전에 중단한다.
- 화면 경계에서 point UIA가 계속 비어 있더라도 거래처 관리항목 Enter를 실제로 보낸 경우에는 추가 Down을 보내지 않고 `last_full_y`를 `enter` 고정 좌표로 확정한다. 마지막 표시행 더블클릭 때 먼저 스크롤되든 Enter 때 스크롤되든 다음 행이 같은 마지막 완전 표시 슬롯에 오는 ERP 동작을 이용한다.
- 화면 표시행은 24/26 등 런타임 경계를 그대로 사용하고 총행은 210으로 고정하지 않는다. 210/230/300행 회귀에서 첫 행부터 동적 마지막 보통예금 행까지 누락 없이 도달하고, 보통예금의 기존 계좌번호/금융기관지점 fast 입력 분기는 변경하지 않았다.
- 배포 저장소 전체 테스트 결과: 76 passed. Python AST 구문 검사와 `git diff --check` 통과. 공식 Graphify 업데이트 완료(2425 nodes, 7001 edges, 111 communities).

## 2026-07-20 GDI 출납처리여부 체크박스 UIA 미노출 중단 수정
- 전표 210행과 마지막 보통예금까지 붙여넣기는 정상 완료됐지만 관리항목 1행 진입 직전에 `1행 출납처리여부 체크박스의 최초 UIA 기준을 찾지 못했습니다.`로 중단됐다. 첫 행 F9보다 먼저 실행되는 출납처리여부 해제 단계가 이름 있는 UIA 체크박스 캐시를 필수 조건으로 둔 것이 직접 원인이었다.
- ERP GDI 모드는 표준 `CheckBox/Check1`을 노출하면서 접근성 이름만 비우는 경우가 있으므로, 최초 준비 스냅샷에서 전표 그리드 하단과 하단 폼 위치를 이용해 무명 `Check1`도 실제 Toggle 객체로 캐시한다. 상태가 체크됨이면 기존처럼 한 번 해제하고, 이미 해제됨이면 클릭하지 않는다.
- GDI가 체크박스 객체를 전혀 제공하지 않거나 캐시가 stale 상태가 되면 작업을 실패시키거나 고정 좌표를 무조건 클릭하지 않는다. 운영 화면의 기본 해제 상태를 유지하고 관리항목 입력을 계속한다. 빈 체크박스를 좌표로 클릭해 오히려 체크 상태로 반전시키는 위험을 막는다.
- 첫 행 단일클릭 → F9 키보드 순서, 2행 이후 거래처 직접 입력, 24/26 표시행과 210/230/300 총행의 런타임 이동, 마지막 보통예금 계좌번호/금융기관지점 로직은 변경하지 않았다.
- 회귀 테스트는 이름 없는 하단 GDI `Check1` 캐시, 정상 캐시의 체크/해제 상태, 캐시 누락 및 stale 상태에서 전체 UIA 재탐색·오류·임의 좌표 클릭 없이 계속 진행하는 동작을 검증한다. 배포 저장소 전체 테스트 결과: 78 passed. Python AST 구문 검사와 `git diff --check` 통과. Graphify 업데이트 완료(2420 nodes, 6968 edges, 109 communities).

## 2026-07-20 마지막 보통예금 계정과목 검증 좌표 수정
- 운영 로그에서 209행 거래처 입력과 210행 이동·적요 더블클릭은 정상 완료됐고, 화면에도 210행 보통예금의 `계좌번호/금융기관지점/거래처`가 표시됐다. 실패는 실제 관리항목 입력 전에 실행한 계정과목 Ctrl+C 검증이었다.
- 기존 `ERP_MGMT_ACCOUNT_X=229`는 운영 GDI 그리드 계정과목 열의 왼쪽 경계여서 클릭 뒤에도 적요 셀이 활성 상태로 남았다. 세 번 모두 `6월 수시결제 - 신한은행`이 복사돼 보통예금 행 선택을 실패로 오판하고 입력 함수를 호출하지 않았다.
- 기존 계정과목 좌표 `x=229`는 유지한다. 좌표를 누르기 전에 현재 포커스 적요를 먼저 복사하고, 그 값이 payload의 해당 행 적요와 정확히 일치하면 현재 보통예금 행이 맞는 것으로 인정해 계좌번호/금융기관지점 입력을 계속한다. 다른 행 적요는 통과하지 않으며, 포커스 적요가 불일치할 때만 계정과목 좌표와 셀 중앙 `ERP_MGMT_ACCOUNT_VERIFY_CENTER_X=295`를 차례로 확인한다. 각 시도 전에는 sentinel을 넣어 이전 클립보드값을 성공으로 오인하지 않는다.
- 행 이동, 첫 행 F9, 미지급금 거래처 입력, 계좌번호 `140-000-948562`와 금융기관지점 `신한 수원금융센터` 값 및 입력 좌표는 변경하지 않았다. 배포 저장소 전체 테스트 결과: 80 passed. Python AST 구문 검사와 `git diff --check` 통과. Graphify 업데이트 완료(2422 nodes, 6990 edges, 107 communities).

## 2026-07-20 마지막 보통예금 GDI Ctrl+C 검증 제거
- 직전 보강 후에도 같은 위치와 같은 `계정과목이 보통예금인지 확인하지 못해` 오류가 반복됐다. 운영 fast 경로에서 마지막 행의 명시 관리항목이 이미 `계좌번호/금융기관지점`임에도, 불안정한 GDI 포커스의 Ctrl+C 결과를 필수 조건으로 계속 사용한 것이 원인이었다.
- `ERP_SKIP_VISIBLE_ROW_SCAN=1` 운영 경로에서는 계정과목/적요 Ctrl+C 검증과 재선택 3회, 해당 실패 문구를 모두 제거했다. payload에 은행 관리항목이 명시된 마지막 보통예금 행에 도달하면 기존 빠른 좌표를 준비하고 즉시 계좌번호·금융기관지점 입력 함수를 호출한다.
- 회귀 테스트는 fast 보통예금 경로가 클립보드 계정과목 검증과 전체 UIA 스캔을 모두 호출하지 않고 은행 좌표 행을 허용하는지 검증한다. 느린 비운영 경로의 보조 검증은 유지했다. 배포 저장소 전체 테스트 결과: 80 passed. Python AST 구문 검사와 `git diff --check` 통과. Graphify 업데이트 완료(2423 nodes, 6991 edges, 111 communities).

## 2026-07-20 관리항목 실제 표시 반복 감지 복원 및 배포 검증
- 예전의 관리항목 준비 대기 자체는 삭제되지 않았다. 운영 로그에서 최초 감지는 `labels=[]`, `visual_ready=False`였고, 다음 감지는 실제 관리항목명이 여전히 없는 `labels=[]` 상태에서 픽셀 점수만 `visual_ready=True`가 되어 준비 완료로 잘못 통과했다. 이 때문에 완전히 열린 관리항목 폼의 1행 기준이 아닌 불완전한 스냅샷이 이후 좌표 계산에 재사용됐다.
- 준비 완료 조건을 픽셀 변화가 아니라 payload 첫 관리항목의 실제 이름으로 복원했다. 첫 관리항목이 `거래처`이면 UIA 스냅샷에서 `거래처`가 실제로 검출될 때까지 기존 제한시간 안에서 반복 감지하며, 픽셀 신호는 진단 로그에만 남긴다. `계좌번호/금융기관지점`처럼 첫 행에 여러 항목이 명시된 경우에는 명시된 항목이 모두 검출돼야 한다.
- Excel 붙여넣기뿐 아니라 텍스트 클립보드 붙여넣기도 같은 반복 감지를 거친 뒤 1행 입력을 시작한다. 마지막 보통예금 fast 좌표는 `semantic_ready=True`로 검증된 최초 스냅샷에서만 준비되므로, 불완전한 초기 화면 좌표를 재사용하지 않는다.
- 243 최신 적용은 큐 등록 직후 완료로 표시하지 않고 Agent 명령이 실제 `done` 또는 `error`가 될 때까지 기다린다. 내려받은 담당자 프로그램과 실제 `C:\ERP_Auto_Web`에 복사된 파일의 SHA-256을 비교하고, 일치한 해시를 완료 화면에 표시한다.
- 배포 저장소 전체 테스트 결과: 83 passed. Python 구문 검사, JavaScript 구문 검사와 `git diff --check` 통과.
