# Debug

Updated: 2026-08-13

## I-001 — backend static frontend dependency was missing — resolved in reconciliation

- The reconciliation commit restored the tracked `web_v1/frontend` assets and retained the later verified `index.html` and `app.js`.
- JavaScript and DOM mapping must be rerun on the integrated branch.

## I-002 — required setup EXE was missing — resolved in reconciliation

- `web_v1/backend/tools/AccountingWebRequiredSetup.cs/.exe` are tracked and present.
- Installer HTTP behavior remains an operational check.

## I-003 — Graphify parents conflict — resolved by regeneration

- Both branch lines changed `graphify-out/GRAPH_REPORT.md`, `graph.html`, and `graph.json`.
- No source file conflicted.
- The remote graph was selected only to finish the cherry-pick.
- The parent JSON was moved to an ignored local backup and the combined graph was rebuilt: 89 files, 1,889 nodes, 5,417 edges, and 34 reported communities.
- Both products and their shared Manager/crawler paths are present; historical/temporary and absolute source-path hits are zero.

## I-004 — branch version claim was wrong — resolved in documentation

- The reconciliation documents claimed WEB/Agent 1.0.228, based on the dirty original worktree.
- Commit `0fd4f46` did not contain `web_v1/VERSION` or most 1.0.228 runtime changes.
- The integrated branch actually contains version 1.0.164. Current documents now distinguish the two lines.

## I-005 — Zoom module is present but inactive — open integration boundary

- `zoom_billing.py` is tracked on the integrated branch.
- The integrated `mail_collector.py` has no Zoom import or routing call.
- The working 1.0.228 collector has 400+ additional changed lines, including retry/state behavior around the Zoom path. Copying only the visible routing block could break its state variables and Seen/failure policy.
- Next action: port the complete reviewed mail-collection feature unit with focused tests in a separate versioned change.

## I-006 — dirty original worktree — preserved

- The original `main` remains one commit ahead and 185 commits behind its configured remote, with extensive tracked and untracked user changes.
- Integration work occurs under an ignored isolated clone. No original tracked file is modified, stashed, reset, or rebased.

## I-007 — deployment E2E — open verification boundary

- Not run: live FastAPI startup, installer response, mailbox/groupware access, Agent claim/complete, ERP GUI, printer, voucher-number recovery, and output completion.
- Reason: these paths require deployment credentials, Windows GUI state, printers, and `C:\ERP_DB`, and may start schedulers.

## I-008 — patch runner path handling — contained

- The direct patch tool stalled when asked to traverse to the initial temp clone.
- The clone was moved under the repository's ignored `_codex*` workspace and the same patch engine was invoked with relative paths.
- Runtime source files were not edited by this workaround.

## I-009 — initial pytest collection path — resolved

- Running pytest from the repository root produced four `ModuleNotFoundError: app` collection errors because the subsystem expects `excel_voucher_web` as its working directory.
- Rerunning from `excel_voucher_web` used the documented import layout and passed all 186 tests.

## I-010 — static DOM audit — resolved

- The HTML contains 88 IDs and `app.js` has 88 static ID selectors.
- Four selectors are intentionally generated by JavaScript: `adminDbButton`, `outputSetPanel`, `savedOutputTarget`, and `outputPrinterKey`.
- The remaining 84 selector IDs exist in static HTML.
